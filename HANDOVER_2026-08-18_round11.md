# 2026-08-18 追加修正（第十一輪）：管理員S排名權限、操行等第級距、Excel下載容錯；AI.xlsx公式驗證重大進展

安裝順序：`sql/49fix_admin_s_ranking_visibility.sql` 需在 sql/48 之後執行。

## 1a. 成績總表下載EXCEL：ChunkLoadError

這種「ChunkLoadError: Loading chunk ... failed」錯誤，絕大多數情況是**網站剛部署過
新版本，瀏覽器頁面還停留在舊版本**造成的——新版本的檔案名稱（含隨機碼）跟瀏覽器
記憶體裡舊頁面預期要抓的檔案名稱對不起來，導致下載那個檔案時失敗（跟前幾輪
「downloadClassScoreExcel is not a function」是同一類問題，只是這次瀏覽器好心
多印了一個明確的錯誤名稱）。這輪把下載功能改成：遇到這種錯誤時，會跳出對話框
問要不要重新整理頁面，不用再自己猜是什麼問題、要不要清快取。之後如果部署新版本，
麻煩告訴大家「如果按下載沒反應或跳出錯誤，重新整理一次網頁再試」，會比每次都要
回報給我快很多。

**下載後直接把你要的格式放新分頁回傳給我**——這個工作方式很好，之後有格式類的
需求都可以這樣做，比我用文字猜你要的樣子準確、來回也少很多。

## 1b. 管理者S看不到總分跟排名，但A可以

**根因**：上一輪（sql/47）我自己重寫「總分/排名」這四個查詢時，圖方便自己寫了一段
判斷「是不是管理員」的邏輯（直接比對角色文字是不是 admin_a/admin_b/system_admin_s），
沒有沿用系統其他所有地方（成績、學籍、出缺勤…幾乎每一張表）統一在用的判斷方式
（`is_system_admin()`／`has_department()` 這兩支「大家都在用、已經驗證過」的函式）。
兩種寫法理論上該讓同一群人看到，但只要有任何一點資料上的不一致，只有我自己另外
寫的那一份會抓不到，其他地方不受影響——這正好對應「S看不到總分排名，其他資料
（科目分數等）都正常」這個現象。已經改成跟系統其他地方統一，使用同一套判斷函式。

如果套用後S還是看不到，麻煩幫我在資料庫執行這一行，把結果告訴我（我需要知道
系統管理員S的帳號在資料庫裡 role 欄位實際存的是什麼值，才能繼續排查）：
```sql
select id, role, name from app_users where role = 'system_admin_s';
```

## 2. 操行等第規則更新

已改成你這次給的正確級距：90以上優、80-89甲、70-79乙、60-69丙、未滿60丁
（上一輪我是用截圖裡「65分=丙」反推猜的級距，不對，這次直接照你給的規則改）。

## 3、4. 課表自動隱藏多餘節數／聘書PDF字型錯誤與批次列印

這兩項這輪還沒有處理——這次同時處理了1、2、5（尤其5的公式驗證意外花了不少
時間，詳見下面），時間上沒有餘裕再展開課表跟聘書這兩個獨立的功能。這兩項各自
都有一定規模（課表要知道你們學校目前的課表結構長怎樣才能判斷「多餘節數」；
聘書PDF的字型錯誤要先找出是哪一種字型/哪個聘書模板觸發的，批次列印是全新
功能），麻煩留到下一輪，或如果你覺得優先，也可以下一輪只先處理這兩項。

## 5. AI.xlsx／0808.xlsm 轉成HTML Table

**重大進展，比原本預期更有價值**：你這次上傳的 AI.xlsx「成績內」那張表**保留了
實際的 Excel 公式**（不是只有數字），這讓我第一次能直接拿你們學校真正在用的公式
逐格核對，而不是像前幾輪只能憑你描述的文字反推、猜完也沒辦法驗證。核對結果：

- 「學業平均」那一列的公式＝各科分數×各科比重，加總後再加上「出缺席分數×出缺席
  比重」——跟這兩輪 sql/48 實作的公式**完全一致**，等於是被你自己的參考檔案驗證過了。
- 「全學年」欄的公式＝期中平均×35%＋期末平均×35%＋平時平均×30%，數學上跟系統
  現在的算法（反過來，先合併期中/期末/平時、再乘上科目比重）其實是同一個答案
  （這是線性加權，換個順序算結果一樣），也對得起來。
- 「操行成績」的公式其實是 `=AVERAGE(禮貌,衣著,服務,紀律)`，**沒有把嘉獎/警告等
  懲獎點數算進去**——但你的成績單「封面說明頁」文字寫的是「操行基本分80分，
  導師可加減至多3分，特殊表現經訓導簽核後嘉獎/警告等再加減分」，這兩個描述並不
  一樣（封面文字說有懲獎加減、實際公式沒有）。目前系統的操行成績計算是照封面文字
  做的（四項平均後再加減懲獎點數），**這個地方需要你確認：實際上懲獎記錄到底要不
  要反映到操行成績上**，我才能確定要維持現在的做法還是改成單純四項平均。

**這輪完成的部分**：因為公式已經驗證一致，這輪沒有必要再去動計算邏輯本身。

**還沒完成的部分（版面本身、以及0808.xlsm聘書三頁）**：把整份 Excel（含合併儲存格、
框線、跟右側直式的公元日期欄位、封面說明頁）轉成像素級一致的 HTML/PDF 版面，
是一個蠻大的量（AI.xlsx 光「成績內」這張表就有近70個合併儲存格範圍），加上
0808.xlsm 另外還有5張分頁（在職證明、自聘教師聘書、當年教師聘書），這輪時間上
沒有餘裕做完整轉換還能維持前幾輪「至少邏輯驗證過」的品質水準；勉強做完但沒把握
的版面調整，上一輪已經證明「沒有辦法預覽、只能靠猜」風險頗高（操行等第級距那次
猜錯就是例子）。

**建議下一輪的做法**：既然 AI.xlsx 已經證明「附公式的 Excel」比純文字描述有效
很多，聘書那三份 (0808.xlsm) 如果你也能提供「有公式/有實際格式設定」的版本
（不用整理過，你們現在在用的原始檔案就好），下一輪我可以比照這次的方式，先
把公式/格式都核對一次列出「跟現在系統的差異清單」給你確認，確認完再動版面，
會比我直接動手改、你事後才發現不對更有效率。
