# 修正說明（2026-08-08 第三輪）

依這輪3項需求逐一處理。以下說明改了哪些檔案、根本原因是什麼、以及上線前要注意什麼。

---

## 1. 「聘書分類」改名「聘書」、新增歷年教師資料自動結算、三個列印功能合併

**a. 改名＋以「0808.xlsm」為樣本確認公式關聯**
- 開發人員頁的區塊標題從「聘書分類」改成「聘書」。
- 原本的「在職證明」改名「歷年教師資料」（分頁標題、範本檔名、下載檔名都跟著改）。
- 用「0808.xlsm」逐一比對「自聘教師聘書」「當年教師聘書」的欄位，跟系統既有的
  `teacher_appointment_letters` 資料表欄位完全對齊，不用調整資料庫結構。
- 追查發現原始活頁簿的3個「印＊」列印頁，其實都是「填一個輸入格（教師姓名或序號），
  其他欄位全部用 VLOOKUP 從資料工作表自動帶出」的設計——這個發現直接決定了下面c項
  合併列印頁的做法。

**b. 歷年教師資料自動結算在職總年數/月數**：
`lib/teacherLetters.ts` 的 `computeServiceDuration()`，逐字比對「0808.xlsm」歷年教師資料
工作表 R/S/T 三欄（年/月/日）的原始公式（DATEDIF系列公式）後重寫成JS版本，並且用
活頁簿裡實際存的12筆快取計算結果驗證：其中9筆完全對上，另外3筆（都是「只有1段任職/
離職紀錄」的已離職教師）比對後發現是**樣本檔案自己內部的快取值跟$D$1(計算日期)不一致**
（快取結果對應的計算日期實際上落在9月初，但$D$1目前顯示是8月8日）——這是樣本檔案
產生當下的既有落差，不是公式邏輯有錯，已經用其餘完全吻合的欄位組合交叉驗證過公式
本身的邏輯是對的。這個試算結果現在會直接顯示在「歷年教師資料」的清單畫面上
（新增「在職時間」欄位），不用另外下載才看得到。

**c. 三個列印功能合併**：新增「列印」分頁（`components/dev-tools/TeacherLettersPanel.tsx`
的 `PrintSection`）。做法不是在網頁裡重新刻一次原本活頁簿的公式邏輯，而是：
1. 讀取範本活頁簿（預設用內建的「0808.xlsm」，可以上傳新樣本取代——**上傳的樣本只在
   當次瀏覽器分頁有效，不會存到資料庫，重新整理頁面就會恢復用內建範本**，如果需要
   永久保存自訂範本，需要再另外加開發）。
2. 把範本裡的「歷年教師資料／自聘教師資料／當年教師資料」這3張資料工作表，整個換成
   資料庫目前最新的資料（欄位、起始列位置都跟原始活頁簿完全對齊）。
3. 把使用者選的教師姓名／序號填進範本原本就有的輸入格（在職證明填B3；自聘教師聘書/
   當年教師聘書填F2）。
4. 下載這份活頁簿，使用者用 Excel 打開的時候，原本活頁簿裡的公式會自動重新計算，
   印出來的格式就是「完全使用0808.xlsx的格式」，不會因為改用網頁而跑掉。

  **目前限制**：一次只產生「一筆」列印檔（教師/序號要一筆一筆選、一份一份下載），
  跟原本VBA�750macro「改一次輸入格、印一次」是同一種操作方式，但沒有做到「一次把
  整個序號範圍通通印在同一份活頁簿裡」的批次功能——如果需要這個，請再提出，
  這部分要另外處理Excel工作表複製＋樣式保留，會是比較大的一塊工作。

**新增/修改檔案**：
- `lib/teacherLetters.ts`：重寫 `computeServiceDuration()`；改名相關常數/檔名。
- `components/dev-tools/TeacherLettersPanel.tsx`：改名分頁標題；「歷年教師資料」清單
  新增「在職時間」欄位；新增「列印」分頁（`PrintSection`）。
- `public/templates/teacher_letters_print_template.xlsm`：內建的預設列印範本
  （直接就是這次上傳的「0808.xlsm」）。
- `app/(app)/admin/dev-tools/page.tsx`：區塊標題改名。

---

## 2. 合併班級功能出現錯誤提示

**根本原因**：`merge-teachers.ts`（合併教師）的程式開頭本來就記錄過一次同類問題：
「只要漏掉一個有外鍵參照的資料表，最後刪除多餘資料時就會被資料庫擋下來」。合併班級這邊
確認過3張有外鍵參照 `classes(id)` 的資料表（`enrollments`／`class_schedule`／
`substitute_assignments`）都有列在改指清單裡，沒有漏掉——但真正的問題是「改指到保留
那一筆」這一步本身會撞到 unique 限制：**兩個要合併的班級通常各自都有一份完整的學生
名單/課表，座號、上課節次幾乎一定會重複**（例如兩邊都有座號1號、星期一第1節），過去
撞到 unique 限制的那幾筆會被跳過、不改指，最後刪除多餘班級時，這幾筆「還沒改指成功」
的資料仍然參照著要刪除的班級、違反外鍵限制、整個刪除失敗——所以幾乎每次合併都會出現
錯誤提示。

**修正**（`app/api/admin/merge-classes/route.ts`）：
- `enrollments`（學生座號撞到）：不放棄，自動改配到保留那個班級（該學期）目前
  最大座號之後的下一個座號，學生資料不會遺失，只是座號變動，合併後建議人工檢查一下
  座號順不順。
- `class_schedule`／`substitute_assignments`（節次/代課撞到）：保留那個班級本來就
  已經有同一個時段的資料了，代表要合併掉那筆是重複資料，直接刪除多餘那筆即可
  （不影響保留那筆的資料）。
- 前端（`app/(app)/admin/class-accounts/page.tsx`）合併完成的提示訊息，補上「有幾筆
  座號被自動改配」「有幾筆重複資料被略過」的說明，方便合併後人工複查。

---

## 3. 班級批次上傳成績不會顯示於排名頁面 + 補上成績上傳時間設定表

這兩個問題其實是同一個根本原因，一次解決。

**根本原因**：`class_rankings`／`grade_rankings`這兩個排名view，是否顯示期中考/期末考/
平時分，要看 `submission_windows` 這張表對應那一格「是否已鎖定(is_locked)」——但追查後
發現：
1. `exam_type_locked()` 這個判斷函式從一開始就**只檢查 `scope='班級'` 那一筆**，
   完全沒有理會 `submission_windows` 表原本設計就有的 `scope='部別'`／`scope='全校'`
   這兩種範圍（等於形同虛設）。
2. 系統裡唯一「鎖定」的操作入口，是導師在「班級成績總表」頁面點的「確認送出並鎖定」
   按鈕——那顆按鈕**只會鎖「平時分、自己班」**，從頭到尾沒有任何畫面可以鎖
   「期中考」「期末考」。

結果就是：老師/管理員批次上傳期中考、期末考成績後，因為永遠沒有任何一筆
`submission_windows` 把該班的「期中考」「期末考」設成已鎖定，排名view就永遠只顯示
空白，看起來就像「成績上傳了，但排名頁面看不到」——這正是回報的兩個問題（成績不顯示、
沒看到上傳時間設定表）背後同一個缺口：**從來沒有畫面可以做這件事**。

已確認：期中考、期末考、平時分的單獨總分與排名，資料庫層面本來就已經各自獨立計算好了
（`sql/10exam_type_rankings.sql`、`sql/21ranking_lock_granularity_fix.sql`），這部分
不需要調整，只是缺一個操作入口。

**修正**：
- `sql/34fix_exam_type_locked_scope.sql`：`exam_type_locked()` 改成「班級鎖定優先，
  沒有班級層級的設定時，往上看部別、再往上看全校」——管理員可以直接鎖「全校的期末考」
  一次搞定所有班級，也保留了「這個班要另外處理（例如緩考、成績有疑義）」時單獨設定
  就會蓋過部別/全校設定的彈性。
- 新增 **`app/(app)/admin/score-submission-windows/page.tsx`**（成績上傳時間設定表）：
  教務處/系統管理員可以設定「全校／部別／班級」三種範圍的開放時間、以及是否鎖定，
  出缺勤則需要訓導處或系統管理員權限（沿用既有的 `submission_windows` 寫入權限規則）。
  已加進導覽選單（`lib/adminModules.ts`）。

**上線前請注意**：
1. **記得先在 Supabase 依序執行 `sql/34fix_exam_type_locked_scope.sql`**
   （接在上一輪的33號之後）。
2. 這個修正之後，**期中考/期末考的排名要「鎖定」之後才會顯示**是系統原本就有的設計
   （避免老師還在陸續輸入成績時，排名顯示不完整的資料），不是bug——所以批次上傳完
   成績後，請到新的「成績上傳時間設定表」頁面，把對應的學年度/學期/類別鎖定，
   排名頁面才會顯示。建議教務處在成績登錄截止後，統一到這個新頁面「全校」鎖定一次
   即可，不用每班分別設定。

---

## 測試方式

已用 `npx tsc --noEmit` 及 `npx next build` 驗證過整個專案，這3項新增/修改的檔案都沒有
型別或建置錯誤（剩下2個錯誤是 `app/api/reports/report-card` 底下既有、跟這幾次修改完全
無關的舊問題，前兩輪交接文件都記錄過同樣的問題）。資料庫層面的行為（合併班級的座號改配、
排名鎖定生效與否、聘書列印檔下載後用Excel打開公式是否正確重算）建議部署、跑完
`sql/33teacher_letters.sql`、`sql/34fix_exam_type_locked_scope.sql` 之後，用測試帳號
實際操作一次再正式上線。
