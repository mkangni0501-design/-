# 2026-08-14 修正紀錄：【成績登錄】頁 + 【科目與比重設定】頁

這一輪對應的反映事項（原始編號沿用）：

## 1a. 下載範本改成真實名冊＋真實科目
- `lib/excelTemplates.ts` 新增 `buildScoreAttendanceSheetForClass()` /
  `downloadScoreAttendanceTemplateForClass()`：帶入真實班級名冊（座號、學號、姓名）
  跟真實科目清單，取代原本完全空白、跟班級無關的示範資料（`downloadScoreAttendanceTemplate()`
  仍保留，未選班級時使用）。
- `ScoresEntryTab.tsx` 的「下載範本」按鈕：已選班級時改叫上面新增的函式；科目清單
  用 `sortedSubjectOptions`（本來就已經依「是否為自己任教科目」過�former濾好）。
- 上傳端：`handleUploadFile` 新增檢查，非管理員S/A/B、非教務處主管的人上傳時，
  只要科目不在自己教的科目清單裡就跳過並提示錯誤（資料庫層級 `can_write_score()`
  本來就會擋，這裡加在前端讓訊息更清楚，見 `sql/41score_entry_fixes.sql`）。

## 1b. 鎖定跟儲存分開，且期中/期末/平時各自獨立
- `submission_windows` 資料表本來就是以「班級＋考試類型」為單位各自一筆
  （`sql/34fix_exam_type_locked_scope.sql` 已修過 fallback 範圍），但過去**沒有任何
  畫面**讓任課教師針對「期中考」「期末考」按下鎖定（只有導師在班級總表能鎖「平時分」）。
  這次在 `ScoresEntryTab.tsx` 新增「🔒 鎖定」按鈕，任何有寫入權限的老師存完分數後
  都能對「目前這個班＋目前這個考試類型」按下鎖定；鎖定狀態存在資料庫，離開頁面
  再回來會重新從資料庫讀，不會自動解除。
- **注意**：鎖定的粒度是「班級＋考試類型」，不是「班級＋科目＋考試類型」——鎖定後
  這個班這次考試的**所有科目**都會被鎖住，UI 上的確認對話框已經明講這件事。

## 1c. 個人成績單無法預覽/列印
- 根本原因：`lib/reportCard.ts` 的 `buildReportCardData()` 要求期中/期末/平時分
  **三項都鎖定**才會產生正式成績單，API 失敗時有回傳 `reason` 說明原因，但前端
  （`ClassSummaryTab.tsx`）原本只顯示「產生成績單失敗」，看起來像整個功能壞掉。
  兩處都已改成把 `reason` 顯示出來。
- `ScoresEntryTab.tsx` 新增「個人成績單」區塊（僅導師本人／管理員看得到），選班級後
  就能看到名冊，可個別預覽/列印，不用切去班級總表頁。
- 這個問題會隨著 1b 的鎖定功能補齊而自然改善（能鎖三項之後，成績單就能正常產生）。

## 1d. 科目排序依比重高到低＋科目間有間隔線
- `ScoresEntryTab.tsx`／`ClassSummaryTab.tsx` 的科目清單都改成依 `curriculum.weight`
  由大到小排序（沒有比重設定的排在最後，維持原順序）。
- `ScoresEntryTab.tsx` 的科目下拉選單在每個科目之間插入一個 `disabled` 的分隔線選項；
  `ClassSummaryTab.tsx` 本來就有 `borderLeft` 當欄位分隔，維持不變。

## 1e. 期中/期末/平時各自總分沒有乘比重、沒有平均
- `sql/41score_entry_fixes.sql` 重建 `student_examtype_totals`：
  - `{examtype}_total` 改成「該科分數 × curriculum.weight」加總（原本是單純加總原始分數）
  - 新增 `{examtype}_average`：該次考試「有登錄成績的科目」原始分數平均
  - 兩者都排除 `curriculum.weight = 0` 的科目（對應第 3 點）
- 連帶重建 `class_rankings` / `grade_rankings`，在原本欄位後面補上三個 average 欄位
  （沒有更動既有欄位順序，只在後面 append，降低影響既有查詢的風險）。
- `ClassSummaryTab.tsx` 總表新增「平均」欄位，跟「總分(*比重)」並排顯示。

## 1f. 鎖定又解開後，教師修改的內容要留下紀錄（僅S/A看得到）
- 發現 `score_audit_log` 這張表從建立以來**從未被任何程式或 trigger 寫入過**，等於
  完全沒有在記錄。`sql/41score_entry_fixes.sql` 補上 `trg_log_score_change` trigger
  （比照 `sql/40score_attendance_audit_and_autolock.sql` 對 attendance 的做法），
  在資料庫層級對 `scores` 表的 INSERT/UPDATE/DELETE 一律自動記錄，不會因為前端漏寫
  某個寫入管道而少記。
- `app/(app)/admin/audit-logs/page.tsx` 新增「成績修改紀錄」分頁，讀取
  `score_audit_log`（RLS 已限制僅 `system_admin_s`／`admin_a` 查得到，畫面上的角色
  判斷只是第二層防護）。

## 3. 科目與比重設定：比重=0 不顯示、ENTER 換下一個科目
- `ScoresEntryTab.tsx`／`ClassSummaryTab.tsx`／`lib/reportCard.ts`：比重=0 的科目
  都已從「科目下拉」「班級總表欄位」「成績單」濾掉（DB 層面 `student_examtype_totals`
  也排除，見上面 1e）。
- `CurriculumSettingsTab.tsx`：比重輸入框改成用 `ref` 陣列記錄每一列，`onKeyDown`
  攔截 Enter 直接把焦點移到下一個科目的比重輸入框（`focusNextWeightInput()`）。

## 安裝順序
`sql/41score_entry_fixes.sql` 需要在 `sql/1schema.sql` 到 `sql/40...sql` 全部執行過
後再執行；內容都用 `create or replace` / `drop ... if exists` 寫法，可重複執行。

## 已知限制／未驗證事項（無法連線正式資料庫，僅完成語法檢查與程式邏輯比對）
- `class_rankings` / `grade_rankings` 重建時手動抄了 `sql/21ranking_lock_granularity_fix.sql`
  的邏輯再加欄位，**沒有實際跑過**，正式環境套用前建議先在測試環境跑一次確認語法/權限無誤。
- `score_audit_log.score_id` 沒有 `on delete cascade`，分數被刪除後那筆稽核紀錄的
  `score_id` 會指向不存在的資料，畫面上已處理成顯示「（找不到，可能已刪除）」，
  跟 `attendance_audit_log` 對已刪除紀錄的既有限制一致，非本次新增的問題。

---

# 2026-08-14 追加修正：鎖定權限規則 + 平均分算法說明

## 問題 1、2：管理者鎖定後排名仍不出現、總表沒有總成績/排名
根本原因是 `sql/22department_policy_rewrite_complete.sql`（這次修正之前就存在的檔案）
把 `submission_windows` 的管理權限政策改壞了：新的政策要求 `admin_a`／`admin_b`
除非「另外也被指派到教務處(academic)部門」，否則完全沒有寫入權限——但只有
`system_admin_s` 會通過 `is_system_admin()`。Supabase 的 RLS 對 UPDATE 是「條件不符
就影響 0 筆、不會報錯」，所以管理者點「鎖定」畫面上不會看到任何錯誤，但那筆設定
其實完全沒被改到，導致期中/期末/平時三項「看起來鎖了」但排名/總成績還是不出現；
只有導師透過另一個更早、範圍更窄的政策（只能鎖「平時分」）能真的寫入成功，所以才
會出現「要導師鎖平時分才有排名」這種現象。

修正見 `sql/42fix_submission_windows_admin_permission.sql`：
- 管理者 S/A/B 一律可以管理任何範圍/類別的鎖定設定，不再需要額外掛教務處部門標籤
  （教務處主管、訓導處主管仍然照舊可以個別管理自己負責的類別）。
- 導師鎖定自己班級的權限，從「只有平時分」擴大成「期中考／期末考／平時分」都可以
  （三筆各自獨立的設定，不會互相影響）。
- `ScoresEntryTab.tsx` 的「鎖定」按鈕同步只顯示給「有權限鎖定的人」看（導師本人、
  管理員S/A/B、教務處主管），一般只教一科的任課教師看得到「儲存」但看不到「鎖定」
  ——避免顯示一個按了會被資料庫悄悄擋下、卻沒有任何錯誤訊息的按鈕。

`sql/42...sql` 需要在 `sql/41score_entry_fixes.sql` 之後執行。

## 問題 3：期中／期末／平時的「平均」怎麼算？
在 `sql/41score_entry_fixes.sql` 的 `student_examtype_totals` view 裡：

```sql
round(avg(ss.midterm) filter (where ss.midterm is not null), 2) as midterm_average
```

也就是：**這次考試「有登錄成績的科目」，原始分數的算術平均，四捨五入到小數點後兩位**。
具體規則：
- 只計入「有登錄分數」的科目（沒登錄的不計入分母，不是當 0 分計算）。
- 只計入 `curriculum.weight > 0` 的科目（已停開/停用的科目不計入，跟成績登錄頁、
  成績單「不顯示比重=0科目」的規則一致）。
- **平均是原始分數的平均，沒有乘上科目比重**——這是刻意跟旁邊的「總分」分開設計：
  - 「總分」= Σ(該科分數 × 該科比重)，反映的是「照課程權重算出來的整體表現」，
    用於排名。
  - 「平均」= 有登錄科目的原始分數算術平均，反映的是「這次考試考得好不好」的直覺
    印象，不受各科比重影響。

舉例：某生期中考「國文90（比重0.3）、數學70（比重0.2）、英文80（比重0.2）」，
比重合計0.7（其餘科目比重加總為0.3但都還沒登錄分數）：
- 平均 = (90+70+80)/3 = **80.00**（只看有登錄的3科，不管比重）
- 總分 = 90×0.3 + 70×0.2 + 80×0.2 = 27+14+16 = **57**（比重沒登滿，本來就不會是100分制的完整總分——所有科目都登錄完、鎖定後這個總分才有意義，這也是為什麼要等「該考試類型全部鎖定」才顯示的原因之一）

---

# 2026-08-14 追加修正（第三輪）：成績表格線、下載檔分年級、鎖定權限收斂、成績單列印驗證

## a. 成績表格線：科目間粗線、科目內期中/期末/平時細線
`ClassSummaryTab.tsx`（班級成績總表）的表格新增兩個樣式常數：
- `SUBJECT_DIVIDER = '2px solid #2C2C2A'`：每個科目欄位群組（含表頭、表身）最左側
- `EXAMTYPE_DIVIDER = '1px dotted #ccc'`：同一科目內 期中/期末/平時 三個子欄位之間

套用範圍含表頭兩列（科目名稱那列、期中/期末/平時子標題那列）跟表身每一格，讓「這一格
是不是新科目的開始」在視覺上清楚分開。

## b. 科目與比重設定下載檔：不同年級不共用一列
`lib/currentDataSheets.ts` 的 `fetchCurrentCurriculumSheet()`：分組 key 從 `weight|periods`
改成 `grade_level|weight|periods`，不同年級即使剛好比重、節數數字相同，也不會被合併到
同一列；同一年級底下如果有好幾科剛好比重節數相同，仍然可以合併（不影響「調整某一個
年級」時的獨立性）。下載出來的列另外依年級排序，方便閱讀。

**注意**：上傳格式本身沒有改（範本仍然支援一列涵蓋多個年級的精簡寫法），只有「下載目前
現況」這個方向的輸出行為改變，符合題目「下載檔」的字面意思。

## c. 教師鎖定權限必須比管理者小
往下追查發現兩層問題：

1. **導師的鎖定功能其實只在「班級成績總表」頁提供，而且是寫死只認「平時分」**
   （`handleLockDailyScore()` 只 upsert `data_type: '平時分'`），跟上一輪在「成績登錄」頁
   新增的三種類型鎖定是兩套各自為政的程式碼。這次把「班級成績總表」頁也改成
   期中考／期末考／平時分三個各自獨立顯示鎖定狀態、各自一個鎖定按鈕
   （`handleLockExamType(et)`），且鎖定狀態改用 `submission_window_locked()` 這支
   有考慮「班級 > 部別 > 全校」fallback、也有考慮 `closes_at` 到期自動鎖定的函式來查，
   不再只查「這個班自己有沒有設定」——這樣管理者在部別/全校層級鎖定、或設定的
   開放時間已經過了，這裡會立刻反映成「已鎖定」，導師的鎖定按鈕會自動消失、
   不會顯示成「還可以按」。

2. **更關鍵的：上一輪(`sql/42`)寫的導師鎖定政策用 `for all`，其實比文件上寫的
   「鎖定後任何人（含你自己）都無法直接修改」還寬鬆**——`for all` 沒有限制
   `is_locked` 這個值，導師理論上可以直接把自己班的鎖定設定改回 `false`（解鎖），
   或甚至整列刪除，等於導師的鎖定權限其實跟管理者一樣大。`sql/43restrict_teacher_lock_one_way.sql`
   把政策拆成「只能新增/更新成 `is_locked = true`」兩條，不給 DELETE 權限——
   導師只能「鎖」，不能「解鎖」；要重新打開，只能由管理員透過
   「成績上傳時間設定表」頁的「解鎖」操作（會留稽核紀錄，見 sql/40）。

`sql/43restrict_teacher_lock_one_way.sql` 需要接在 `sql/42` 之後執行。

## d. 成績單列印是否確認可行？
**這裡要老實說明限制**：我沒有連線到你的正式資料庫，沒有辦法真的登入系統走一次
「鎖定三項 → 印成績單」的完整流程來眼見為憑。以下是我針對整條路徑，逐段對照程式碼
邏輯做的靜態檢查，確認每一段銜接都是通的：

1. `class_rankings.total_score` 只有在 `exam_type_locked(...,'期中考')` 且
   `exam_type_locked(...,'期末考')` 且 `exam_type_locked(...,'平時分')` 三個都成立時
   才不是 null（sql/41，邏輯沿用自 sql/21，這次沒有改動這段判斷式本身）。
2. `exam_type_locked()`／`submission_window_locked()`（sql/40）會依「班級 > 部別 > 全校」
   查，只要任一層級 `is_locked=true` 或 `closes_at` 已過期，就視為鎖定。
3. 上一輪 + 這次的 `sql/42`、`sql/43` 修正後，管理員S/A/B、教務處主管、導師（自己班）
   都能實際把設定寫進 `submission_windows`（先前是管理員的寫入被 RLS 悄悄擋下、
   導師的鎖定又被限制在只有平時分——這兩個問題現在都解了）。
4. `lib/reportCard.ts` 的 `getReportCardResult()` 讀 `class_rankings.total_score`
   （用 `supabaseAdmin`，不受 RLS 影響，讀取本身沒有權限問題），非 null 才會組出
   `ready:true` 的完整成績單資料。
5. `app/api/reports/report-card/[enrollmentId]/route.tsx` 呼叫
   `canAccessClass()` 做權限檢查（管理員／教務處/academic部門／該班導師本人才能拿到），
   通過後才組 PDF 回傳。
6. `ClassSummaryTab.tsx`／`ScoresEntryTab.tsx` 的「預覽/列印」按鈕呼叫這支 API，
   失敗時已經改成把後端回傳的 `reason` 顯示出來（不會再只看到「產生成績單失敗」）。

以上 6 段連起來，邏輯上應該是通的。**但強烈建議實際跑一次驗證**：找一個測試班級，
把期中/期末/平時都鎖定後，直接去點「預覽/列印」，確認真的能開出 PDF；如果還是不行，
麻煩把畫面上顯示的「原因」文字（alert 訊息）截圖給我，那會直接告訴我是哪一段斷掉，
比我在沒有資料庫的情況下用猜的準確很多。

---

# 2026-08-14 追加修正（第四輪）：用真實資料快照測試後找到的根本問題

這一輪你提供了 `校務系統完整資料快照_20260814.xlsx`，我直接用裡面的真實數字（約9449筆
成績、1334位學生、35個班）逐段對照程式碼邏輯檢查，抓到三個之前沒發現、比較深層的問題。

## a. 成績單一直顯示「三項尚未鎖定」——即使確實都鎖定了
**根因**：`class_rankings`／`grade_rankings` 這兩個 view 最後都有一段「只有本人看得到
自己相關資料」的限制，判斷邏輯是讀 `auth.uid()`（目前登入者是誰）。但
`lib/reportCard.ts`（產生成績單的程式）是用 `supabaseAdmin`（service_role金鑰）查
這兩個 view——**service_role 呼叫沒有登入者身分，`auth.uid()` 永遠是 null**，導致
這兩個 view 對成績單產生程式來說永遠查不到任何一列資料，不管實際上到底鎖定了沒有。

這是系統**從最初就存在**的問題，不是這幾輪修改造成的——也就是說，成績單這個功能
很可能從來沒有真的成功運作過。

**修正**：新增三個專門給伺服器端用的資料庫函式（`report_card_ready`／
`report_card_class_rank`／`report_card_grade_rank`），不依賴登入身分判斷（因為呼叫
前 API 已經另外檢查過權限），`lib/reportCard.ts` 改呼叫這三支函式。

## b. 【班級成績結果與排名】逾時（statement timeout）
**根因**：`class_rankings`／`grade_rankings` 是對「全校」所有學生一起計算排名，
前端再篩選要看的班級/學生——但資料庫沒辦法把「只看一班」的篩選條件往下推進好幾層
疊起來的 view 跟 window function 裡面，等於每次查詢都要把全校資料算完排名才篩選。
用這次快照的資料量實測，全校排名的計算量已經大到會觸發逾時。

**修正**：新增 `class_rankings_for_class()`／`grade_rankings_for_class()` 兩個
資料庫函式，一開始 join 就直接篩選班級（年級排名篩選到「同年級同部別」），不用
等全校算完，效能不會被全校資料量拖慢。【班級成績結果與排名】頁、【班級成績總表】頁
都已經改成呼叫這兩支函式。

**這兩個函式的參數除了 `class_id` 還多收一個 `term`**：追查時發現同一個 `class_id`
在系統裡可能同時存在「上學期」「下學期」兩批獨立的學籍資料列，如果不指定學期，
會把兩個學期的學生誤植在一起排名，名次會是錯的——這是我在寫這兩個新函式時發現
並且已經處理好的細節，原本的 `class_rankings`／`grade_rankings` view 沒有這個問題
（原本就有用 `partition by ...,e.term` 正確區分）。

## c. 管理者看【班級成績總表】仍可點擊已鎖定班級的鎖定按鈕
**根因**：上一輪新增的鎖定狀態查詢，寫的是 `if (academicYear && currentTerm)`——
`academicYear` 是 React 的 state 變數，但 `setAcademicYear(...)` 是非同步的，在
「同一次」畫面重新整理（同一次 useEffect 執行）當下讀這個 state 變數還是舊值
（第一次載入這個班級時甚至還是 `null`），導致這個判斷式整段被跳過、鎖定狀態
永遠查不到，畫面才會一直顯示「還可以點的鎖定按鈕」。已經改成用同一段程式碼裡
剛查好的本地變數 `yearForQuery`（不是 state），這個值是「立刻可用」的最新資料。

## 用真實資料發現的兩個資料設定疑慮（不是程式錯誤，需要你確認）

用這份快照測試「c」的修正時，發現兩件事，這些不是我能替你決定的設定，**想先跟你
確認該怎麼處理**：

1. **「整體佔比與加扣分規則」目前期中比重／期末比重／平時比重都是 0**
   （快照裡「整體佔比與加扣分規則(現況)」那張表第一列是 `0, 0, 0`）。
   這三個數字是算「總分」用的（總分＝期中分數×期中比重＋期末分數×期末比重＋
   平時分數×平時比重，再乘上各科比重）——**只要這三個比重是 0，不管期中/期末/平時
   有沒有鎖定、不管科目比重設得多仔細，每個學生的總分永遠都會是 0**，不是我這次
   任何修正能解決的，因為程式邏輯上這是「正確地把 0 乘出 0」，不是計算錯誤。
   麻煩到【成績相關設定及查詢】裡的「整體佔比與加扣分規則設定」頁，把這三個比重
   改成你們學校實際的比例（例如期中30%、期末30%、平時40%之類），加總通常會等於1。

2. **幼兒園（幼甲/幼乙）、初一、初二、初三 這幾個年級，科目比重幾乎全部是 0**
   （幼甲6科全部0、幼乙6科全部0、初一9科全部0、初二11科有10科是0、初三12科有
   11科是0——只有國小1~6年級跟高一/高二/高三大部分科目有設定真實比重）。
   這是上一輪你要求的「比重=0的科目,成績輸入、成績單不顯示」——如果這是你們
   還沒設定完成、之後會補上真實比重的過渡狀態，那沒有問題；但如果就是這樣沒錯
   （例如幼兒園本來就不打算用加權排名），那麼**這幾個年級的老師打開「成績登錄」頁，
   科目下拉選單目前會是空的，完全沒辦法選科目輸入分數**——這應該不是你要的結果。
   麻煩確認一下：這幾個年級是「比重還沒設定完成」，還是「本來就不需要比重」？
   如果是還沒設定完成，麻煩到「科目與比重設定」頁補上；如果是本來就不需要比重
   （例如低年級或幼兒園不打算做加權排名），跟我說一聲，我再調整規則，讓「沒有
   設定比重」跟「比重刻意設成0」分開處理（例如：科目沒有比重資料時，維持原本
   全部顯示；只有明確被設成0的科目，才不顯示）。

---

# 2026-08-15 追加修正（第五輪）：成績單中文亂碼、版面比照樣本重做、鎖定按鈕觀感

你這輪提供了 `AI.xlsx`（成績單正面樣本），我逐格拆解樣本版型（用 LibreOffice 轉圖檢視
精確欄位/合併儲存格結構），並用真實的高三忠資料（科目/比重直接取自快照）實際產生過
PDF、用 pypdf 反向擷取文字驗證，確認中文不再是亂碼、數字正確。

## 1. 成績單中文亂碼
**根因**：`lib/ReportCardDocument.tsx` 從頭到尾**沒有註冊任何中文字型**（程式裡的註解
自己寫著「這裡先用內建字型示意版型結構」）——`@react-pdf/renderer` 內建字型不含中文字，
所以除了數字（ASCII）以外全部印出來是空白方框/亂碼。

**修正**：
- 從 Google Fonts 官方 Noto Sans TC 可變字重字型抽出 400(Regular)/700(Bold) 兩個固定
  字重（用 fonttools 處理），放進 `public/fonts/NotoSansTC-Regular.ttf` /
  `NotoSansTC-Bold.ttf`。
- 新增 `lib/pdfFonts.ts` 共用註冊函式，用 `process.cwd()` 組出正確的伺服器端絕對路徑
  （不是網址路徑）。
- 另外發現 `lib/TeacherLetterPdfDocuments.tsx`（教師聘書/證明信PDF）**也有同一個問題
  的另一個獨立版本**：雖然有嘗試註冊字型，但寫的是瀏覽器用的網址路徑
  `/fonts/NotoSansTC-Regular.woff2`——這支程式是在伺服器端 Next.js API route 執行，
  不是瀏覽器，這種開頭斜線的字串在 Node.js 裡會被當成「檔案系統絕對路徑」，一般部署
  環境根本沒有 `/fonts/...` 這個路徑，字型讀不到，一樣會印出亂碼。兩個檔案已經統一
  改成呼叫 `registerNotoSansTC()`。
- 已經實際產生 PDF、用 pypdf 反向擷取文字驗證中文字正確嵌入（不是亂碼），檔案大小
  合理（因為 react-pdf 只會嵌入實際用到的字，不是整包字型塞進去）。

## 2. 成績單版面比照樣本重做（用高三忠測試）
把 `lib/ReportCardDocument.tsx` 整個重寫，比照你提供的樣本：科目/比重 + 上學期/下學期
各自的期中/期末/平時/總分 + 學年成績 + 學業平均 + 操行成績(含禮貌/衣著/服務/紀律) +
出席記錄 + 懲獎記錄 + 全班人數/全班名次 + 升留級 + 家長簽章及建議 +
導師/訓導/教務/校長簽章 + 導師評語。`lib/reportCard.ts` 也整個重寫，改成一次抓「這個
學生同一學年度」兩個學期的資料（哪個學期還沒有資料就整個留白，不會是0或錯誤訊息）。

版面設計時發現「學業平均」欄位其實就是「各科分數依科目比重加權平均」——直接用
`student_examtype_totals`／`student_total_scores` 已經算好的數字，不用另外重算
（用樣本的數字反推驗證過：國文35%+英文15%+數學20%+電腦5%+歷史10%+書法4%+才藝4%+
樂斯屬4%+出缺席3%，"期中"欄加權平均剛好等於樣本上顯示的80，"總分"欄加權平均剛好
等於樣本上顯示的77.25，兩個都對得起來）。

**用高三忠真實資料實測**：直接用真實的高三忠課程設定（國文35%/數學20%/英文15%/
常識9%/華測5%/電腦5%/書法4%/才藝4%/全勤3%）產生過 PDF，畫面版面、中文顯示、數字
計算都正確（附圖略，可自行用系統實際列印驗證）。

### 有兩個地方目前沒有對應資料來源，先留白，需要你確認：
1. **操行成績的「禮貌／衣著／服務／紀律」四個分項分數**：現有資料庫沒有對應欄位
   （`conduct_events` 只有嘉獎/小功/大功/警告/小過/大過的獎懲「事件」，不是這四個
   類別的直接評分）。目前 PDF 這四格是空白，不是查詢失敗。如果學校真的需要這四個
   分項評分，我可以另外開發一個新的輸入介面（例如導師登入後可以幫自己班的學生打這
   四個分數），但這是新功能，需要你確認要不要做。
2. **升留級**：現有資料庫沒有對應欄位，先留空白（樣本本身這格也是顯示「-」，所以
   空白其實跟樣本一致）。
3. **學校校徽/印章圖案**：沒有拿到圖檔，目前版面上沒有放這個圖，如果需要，麻煩提供
   圖檔（PNG/JPG皆可）。

### 一個需要你確認的計算假設：
出席記錄的「曠課/遲到/病假/事假/公假」次數，目前是用 `attendance` 資料表裡「符合
這個學期日期範圍、狀態不是出席」的**紀錄筆數**去算——但這張表是「每一節課一筆」
（`unique(學號, 日期, 節次)`），不是「每一天一筆」，如果同一天請假涵蓋好幾節課，
目前的算法會算成好幾次，不是算成一天一次。如果你們學校認定「曠課3次」的意思是
「3個不同的日期」而不是「3節課」，麻煩告訴我，我再改成算不同日期數而不是算紀錄筆數。

另外要提醒：這次修正時發現 `lib/reportCard.ts` 原本查出缺勤/懲獎紀錄時**完全沒有
篩選學期或日期範圍**（會把這個學生從入學以來全部的紀錄都算進去），已經一併修正成
用 `academic_terms` 的起訖日篩選這個學期的範圍。

## 3. 鎖定按鈕「感覺同步、要等很久」
追查後確認**不是功能綁在一起**（點期中考的鎖定按鈕，資料庫裡只會寫入期中考那一筆，
不會影響期末考/平時分），是「載入方式造成的觀感」：三個按鈕的鎖定狀態原本是平行送出
三次個別的資料庫查詢，全部查完才「一起」更新畫面，加上查詢完成前預設顯示「未鎖定/
可以點」，重新整理頁面時就會看到三個按鈕先閃一下「都可以點」，過一陣子才「同時」跳成
正確狀態——看起來像是綁在一起，也因為疊了三次個別查詢的網路往返而變慢。

修正：新增 `class_lock_status()` 資料庫函式一次查完三個狀態（原本三次查詢變成一次），
且查詢完成前畫面顯示「鎖定狀態讀取中…」而不是直接顯示可能不準確的按鈕，避免閃爍。

`sql/44fix_report_card_and_ranking_performance.sql` 這個檔案本身也追加了
`class_lock_status()` 這支新函式，不需要另外新增 sql 檔案。

---

# 2026-08-15 追加修正（第六輪）：操行評分介面、升留級顯示時機、總分/平均公式對調

## a. 操行成績「禮貌/衣著/服務/紀律」評分介面
你確認要開發，已新增：
- `sql/44fix_report_card_and_ranking_performance.sql` 追加 `conduct_scores` 資料表
  （禮貌/衣著/服務/紀律，0~100），RLS 規則跟「導師評語」完全一樣（只有該班導師、
  管理員能看/改）。
- 新元件 `components/admin-tabs/ConductScoresTab.tsx`：導師選自己班（管理員可選任一班），
  逐個學生輸入四個分項，「操行成績」欄位即時算給你看（四個分項的平均，不用另外輸入），
  存檔按鈕一次存全班。
- 掛進【成績相關設定及查詢】新分頁「操行成績評分」。
- `lib/reportCard.ts` 已經改成讀這張新表，成績單上的操行成績/禮貌/衣著/服務/紀律
  不再是空白，會顯示真實輸入的分數。

## b. 升留級只在下學期／學年成績出現
`lib/ReportCardDocument.tsx`：「升留級」這個標籤跟格子，改成只有上下學期都有資料
（也就是可以算整年成績）時才顯示；只印上學期時，這格連標籤都是空白，不會顯示「升留級」
三個字。（升留級本身目前仍然沒有資料來源可以自動判斷，這格顯示出來時還是空白，
需要人工填寫——這部分還沒有另外開發，如果需要自動判斷升留級的規則，麻煩告訴我
判斷邏輯，我再另外處理。）

## c. 出席次數算紀錄筆數 —— 確認無誤，維持原樣，不需修改。

## 3. 總分/平均計算公式對調
發現我上一輪（sql/41）把這兩個公式寫反了：「總分」欄位當時是用各科比重加權，
「平均」欄位當時是單純加總平均——跟你這次的說明剛好相反。`sql/45swap_total_and_average_formulas.sql`
已經對調過來：
- 「總分」＝各科分數直接加總，不乘比重
- 「平均」＝依各科比重加權平均

這個修正會反映在【班級成績總表】頁期中/期末/平時的「總分」「平均(*比重)」兩欄
（欄位標籤也一併改成正確描述）。

### 有一個地方我沒有跟著改，需要你明確告訴我要怎麼處理：
成績單「學業平均」列最右邊的「總分」欄（也就是總分/排名的依據，資料庫欄位是
`student_total_scores.total_score`）**目前仍然是「依各科比重加權」算出來的，還沒有
改成「直接加總」**。原因：我拿你原本給的 AI.xlsx 樣本反推驗證過，樣本上那格顯示
77.25，換算下來剛好等於「依各科比重加權平均」；如果照這次的說明整個改成「各科
直接加總」，這個位置的數字會變成 800~900 這個量級（要看科目數），沒辦法跟樣本
上顯示的77.25對得起來，而且這個數字目前也是全班/年級排名的依據——如果改成
直接加總，不同班/不同學生選修科目數不一樣的話，加總出來的量級會不公平，排名可能
會不準。

麻煩告訴我：這個「總分／排名依據」到底要
1. 維持現在的算法（依比重加權，可以公平比較，也對得起你原本的樣本），或者
2. 也改成各科直接加總（那排名依據可能要另外討論該用什麼欄位，因為直接加總的
   總分沒辦法公平比較不同科目數的學生）。

確認後我再處理，這樣才不會在你還沒看過的情況下改動全校排名的計算方式。

---

# 2026-08-16 追加修正（第七輪）：操行ENTER鍵/schema cache錯誤、出缺勤自動加扣分、成績單A4/日期/樣式自訂

## 1. 操行成績 ENTER 鍵 + schema cache 錯誤
- `ConductScoresTab.tsx` 新增 ENTER 換下一格（往右：禮貌→衣著→服務→紀律，到底自動
  換下一位學生）。
- 「Could not find the table 'public.conduct_scores'」：這個錯誤代表 Supabase 的
  API 層（PostgREST）還不知道這張表存在——通常是 `sql/44` 還沒真的執行過，或執行後
  schema cache 還沒刷新。`sql/44`、`sql/45` 結尾都加上了 `NOTIFY pgrst, 'reload schema';`
  減少需要手動處理的機會；如果套用後還是看到一樣的錯誤，麻煩到 Supabase 後台
  「Settings → API」手動點一次「Reload schema」。

## 2. 期中/期末/平時 總分/平均 數字驗證
用高三忠座號1（蔡智強）真實資料驗證：期中總分＝各科原始分數直接加總＝552.5，
平均＝依各科比重加權＝76.255，兩個都跟你提供的數字完全一致，`sql/45` 的公式
不需要再修改。

## 3d/3e/2（後半）. 出缺勤／懲獎自動加扣分——這是這輪最主要的修改
發現 `conduct_point_defaults`（曠課-0.1、遲到-0.02、事假-0.05、嘉獎+1...）這張表
從 `sql/7conduct_defaults.sql` 建立以來，就如同它自己的註解寫的：「目前只是把這份
參考資料存起來，不會自動套用到總成績計算」——這輪第一次真的把它接上去
（`sql/46wire_attendance_and_discipline_adjustments.sql`）：

- 新函式 `attendance_adjustment(enrollment_id)`：完全沒有曠課/遲到/病假/事假/公假
  紀錄時（真正全勤），回傳該年級課程設定裡「全勤」或「出缺席」科目的比重換算成的
  加分（比重*100，例如3%→+3）；只要有任何一筆，改成用 `conduct_point_defaults`
  的點數×次數加總（例如你舉例的 1×0.1+5×0.02+22×0.05=1.3，倒扣1.3）。
  這支函式接進 `student_adjustments`，全班/全校排名依據的總分會自動反映這個加扣分。
- 新函式 `discipline_adjustment(enrollment_id)`：把懲獎記錄（嘉獎/小功/大功/警告/
  小過/大過）的點數加總，接進操行成績（`conduct.overall` = 禮貌/衣著/服務/紀律的
  平均 + 這個加扣分）。
- 成績單新增「出缺席」列（放在科目最下方，符合你的要求）：全勤顯示100分，否則
  顯示 100+倒扣（例如 98.70）。用高三忠座號1的例子驗證：76.255×0.35+76.255×0.35+
  76.255×0.30-1.3=74.955，成績單「學業平均」總分欄正確顯示74.95，「出缺席」列
  正確顯示98.70。
- **重要**：發現高三等年級的課程設定裡，「全勤」目前仍然是一個老師可以手動輸入
  分數的正常科目。如果同時還讓它走原本「比重加權」的算法，會變成同一件事被算
  兩次。已經把 `student_examtype_totals`／`student_base_scores` 都排除掉名稱是
  「全勤」或「出缺席」的科目，改成完全由 `attendance_adjustment()` 負責——即使
  老師還是在成績登錄頁看得到「全勤」這個科目、也還是輸入了分數，那筆分數不會被
  排名/總分採用。**建議**：既然這部分已經自動化，可以考慮把課程設定裡「全勤」這個
  科目的比重改成 0（比重=0的科目不會出現在成績登錄頁下拉選單），避免老師誤以為
  還需要手動輸入——要不要這樣做由你決定，這裡沒有自動幫你改課程設定。
- 如果還有其他年級的「出缺席/全勤」科目用了跟「全勤」「出缺席」不一樣的名稱，
  麻煩告訴我，我再加進排除清單跟自動計分的科目名稱查詢清單裡。

## 3a. 列印日期
最右邊新增直式日期欄，比照樣本用中文數字（例如「公元二〇二六年八月十六日」）。

## 3b. A4滿版
把整體字級、列高、留白都放大了一輪，讓整份成績單自然撐滿整張A4橫向頁面，不會
再有下半頁大片空白。

## 3c. 管理員可下載/上傳成績單樣式
新增「成績單樣式設定」頁（僅管理員看得到）：可以調整顏色（學年度色塊、學期色塊、
底色等7項）、字級/邊框粗細（4項）、文字標籤（23個標題文字，例如可以把「學業平均」
改叫「學期平均分數」）。可以下載目前設定成一份JSON、修改後上傳回來，或直接在
表單裡調整。**特別設計成不會動到資料來源**：JSON 檔案裡只認得顏色/字級/標籤這三類
欄位，資料要顯示哪個學生的哪個分數、怎麼計算，是寫死在程式碼裡的，上傳的檔案
就算被人亂改也改不到這部分，不用擔心會被誤用來竄改成績數字本身。
已經實際測試過套用自訂樣式（改顏色+改標籤文字）產生 PDF，確認顏色、文字正確
套用，資料完全不受影響。

## 這輪還沒處理、需要你確認的部分
1. 上面提到的「全勤」科目重複計分風險——已經在資料庫層面排除，但課程設定本身
   要不要一併調整（比重改0），由你決定。
2. 「出缺席」列的顯示邏輯（全勤100分／否則100+倒扣）是我依照你的說明跟樣本反推
   出來的公式，如果實際還有其他細節（例如倒扣是否有下限、上限，超過100的獎勵
   要不要顯示超過100），麻煩測試後告訴我。
