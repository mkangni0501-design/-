# 2026-08-20 追加修正（第十六輪）：抓到扣分數字被少乘33倍的根因、家長入口排查方向

安裝順序：`sql/52fix_conduct_point_defaults_scale.sql`，跟其他檔案沒有依賴關係，
可以獨立執行；會直接覆蓋 `conduct_point_defaults` 現有的曠課/遲到/事假/病假/公假
數值，如果你已經自己手動調整過這幾項、且不想被覆蓋，麻煩先告訴我。

## 你附的「成績單外頁說明.txt」抓到一個我自己造成、之前完全沒發現的問題

這份文件把「原始分數」跟「換算成最終佔比後的實際影響」兩個數字並列寫出來
（例如「曠課 -3.33分 (-0.10%)」），這個對照方式讓我第一次能精確核對出一個問題：

`sql/48`（把出缺席改成跟其他科目一樣「原始分數 × 3%比重」計算）之後，資料庫裡
`conduct_point_defaults` 存的曠課/遲到/事假數值，其實還是**改版之前那組「已經是
最終影響」的小數值**（-0.1／-0.02／-0.05）——這組數字在新公式裡又被乘了一次3%，
變成 -0.1×0.03=-0.003，影響力只剩預期的三十分之一，幾乎跟沒扣到一樣。這應該是
前幾輪「算出來的數字跟預期對不太起來」系列問題裡，一直沒抓到的其中一塊拼圖。

已修正：把 `conduct_point_defaults` 更新成「原始分數」欄的正確值（曠課-3.33、
遲到-0.67、事假-1.67、病假0、公假0），乘上3%比重後，換算出來的最終影響就會
正確對上你文件裡「(-0.10%)」「(-0.02%)」「(-0.05%)」這幾個數字。

操行等第的級距（優/甲/乙/丙/丁）你這份文件跟上上輪你直接給我的規則一致（第五條
「不滿九十分者為丁等」看起來是文件本身的筆誤，跟前四條矛盾，應該是「不滿六十分」
才對）——這部分上上輪已經照正確的規則（60分為丁的門檻）改過了，這裡沒有再變動。

「操行基本分80分、導師加減至多3分」這個上上輪提到的矛盾，這份新文件完全沒有提到
「基本分80分」這件事，只提到「特殊表現經訓導簽核加扣分」——現在系統的做法（操行
成績＝禮貌/衣著/服務/紀律四項平均，再加減嘉獎/警告等點數）跟這份新文件是一致的，
不用再改。

## 家長／學生查詢入口登入後空白：顯示「還沒有綁定任何學生資料」

我把登入的兩條路徑（有開信箱驗證／關掉信箱驗證）都重新檢查過一次，程式邏輯上
沒有抓到明確的錯誤，但想到兩個很可能的原因，麻煩幫我確認：

**(a) 如果你測試時「信箱驗證」那個開關其實還是勾選著的**（不是關閉狀態）：這種
情況下走的是原本就有的流程——點驗證信裡的連結回來後，畫面**還有第二步**：會看到
一個「請輸入登入代碼、按確認查詢」的表單（不是直接就進去），這一步是真正在做
「把這次登入的帳號綁定到這位學生」，如果這一步沒有按下去，就會一直是「沒有綁定」
的狀態。麻煩確認測試時是否有完整走完這第二步。

**(b) 如果你測試時「信箱驗證」開關已經是關閉狀態**：理論上綁定是伺服器直接處理，
不需要任何額外步驟，如果還是顯示「沒有綁定」，比較可能是這個全新功能本身還有
我沒抓到的問題。

麻煩幫我確認測試當下開關是哪個狀態、以及（如果是狀況a）有沒有走完第二步的
「確認查詢」，這樣我才能確定要往哪個方向繼續排查。
