# 2026-08-21 追加修正（第二十輪）：成績單「自訂模板」——校徽/校園照片上傳、外頁開關、欄寬調整

安裝順序：`sql/53report_card_image_upload_permission.sql`，跟其他檔案沒有依賴關係。

## 這輪做了什麼

先說清楚範圍：這**不是**真的拖曳定位編輯器（在瀏覽器裡用滑鼠拖每個區塊到任意
位置那種）——那個是完全不同量級的工程（需要把整個版面拆成可任意定位的元件、
做一個畫布、存座標設定檔），這輪沒有做那個。這輪做的是：**在【成績單樣式設定】
頁新增一組「版面配置」選項**，把幾個「風險可控、值得讓你自己調」的項目開放出來，
不用改程式碼、不用等我，存檔後下一次列印就套用：

1. **校徽圖片上傳** — 之前外頁的校徽一直是色塊佔位（沒有圖檔），現在可以直接
   在【成績單樣式設定】頁上傳圖片，上傳後成績單外頁會自動改用真的圖片。
2. **校園照片上傳** — 同上，「敦品勵學　崇德養志」那張背景照片也可以自己上傳。
3. **外頁開關** — 「列印外頁（封面說明頁）」可以勾選/取消，如果之後不想印外頁、
   只要內頁的成績表，取消勾選就好，不用改程式碼。
4. **內頁三個欄位寬度可調** — 「科目」欄、「比重」欄、「學年成績」欄的寬度（佔整
   個表格寬度的百分比）現在可以自己輸入數字調整，例如覺得「學年成績」欄太窄，
   自己把數字調大一點，存檔後就會套用。

這幾個都已經**實際跑過 PDF 產生測試**確認可以正常運作（含「不印外頁」「改欄寬」
這兩個新選項各自測過一次，PDF 都正常產生，不會壞版面）。

## 這次也順手修了一個新舊功能之間的權限落差

【成績單樣式設定】頁原本管理員A/B/S三種身分都能用（改顏色/字級/文字），但這輪
新增的「上傳圖片」功能，借用的是系統既有的共用圖片空間，那個空間原本**只有
系統管理員S**能上傳（是背景音樂功能留下的舊設定，從來沒有對其他管理員開放
過）——如果不修正，管理員A/B會發現自己能改顏色字級，但上傳圖片會失敗，而且
不會有清楚的錯誤說明。已經另外開一條規則，讓「成績單相關的圖片」這個路徑
維持跟成績單樣式設定本身一致的三種管理員都能用，其他既有用途（背景音樂等）
的權限沒有被動到。

## 老實話：為什麼選這個做法，不是拖曳編輯器

拖曳編輯器最大的風險是「我做完你才發現版面亂掉」——因為我這邊沒辦法像操作
Word/PowerPoint 一樣即時看到拖曳的結果，一個完整的拖曳系統需要相當多來回才能
穩定，且做壞了風險比現在這種「表單填數字」的方式高很多。這次選的這組功能
（圖片上傳、開關、欄寬數字）**每一項都是獨立、低風險的調整**，就算某一項你覺得
不好用，也不會牽連到其他部分或讓整份成績單版面跑掉。

如果你實際用過這組功能後，還是覺得需要更細緻的版面控制（例如某個欄位的順序、
某個區塊的間距），麻煩具體告訴我是哪個項目——如果是「常見、大家都會想調」的
項目，可以比照這次的做法再加一個表單選項進去；如果真的需要任意拖曳定位，
那就是前面說的那個大功能，需要另外排一整輪專門做，也想先跟你確認這個投入
划不划算（多數學校的成績單版面調整需求，這輪加的這幾項＋之前已有的顏色/字級/
文字標籤，通常已經能涵蓋大部分情境）。
