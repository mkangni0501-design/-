# 交接文件 2026-08-11：課表查詢／出缺席登錄／代課系統全面看不到資料的修正 + 當年教師資料規則調整

這輪處理使用者回報的 4 個問題。前 3 個問題表面上是「完全看不到資料」，但實際
追查後根因都不是同一件事，分別記錄如下；程式已改完並跑過 `tsc --noEmit` 確認
沒有新增的型別錯誤（`app/api/reports/report-card/` 底下那 2 個既有錯誤跟這輪
修改的檔案無關，這輪沒有動過那兩個檔案）。

## 1.「查詢教師/班級課表」所有帳號完全看不到排課

**根因**：`app/(app)/schedule-lookup/page.tsx` 原本直接呼叫
`supabase.rpc('current_academic_term')` 拿「目前生效」的學年學期，如果還沒有人
到「學年學期設定」（`/admin/academic-terms`）頁按過「設為目前生效」，這個 RPC
就會查無資料，`academicYear`／`term` 會一直是 `null`，而查課表的 `useEffect`
一開始就用 `if (!selectedId || academicYear == null || !term) return;` 擋下——
不是查詢邏輯錯，是**根本沒有執行到查詢**，畫面上就是選了老師/班級也永遠是空的，
而且沒有任何錯誤訊息可以判斷原因。

**修正**：
- 新增共用函式 `lib/academicTerm.ts` 的 `resolveCurrentTerm()`：先呼叫原本的
  RPC，查無資料時依序退回「`academic_terms` 裡狀態=進行中的最新一筆」→
  「學年度最新的一筆（同學年度優先抓下學期）」，都查不到才回傳 `null`。這樣
  即使沒有人手動設定「目前生效」，頁面也能先用最合理的猜測正常運作；之後有人
  正式設定了，就會改用那一筆。
- `schedule-lookup` 頁改用這個函式。
- 另外修正一個連帶的小問題：查「班級課表」時，原本一律用「目前生效學年度」去
  篩 `class_schedule`，如果選的是一個不同學年度的班級（例如去年的班級），會
  因為學年度對不起來而永遠查到空結果。改成查班級課表時優先用「這個班級自己的
  學年度」。

## 2.「學生出缺席登錄」所有帳號看不到自己每日正確授課班級/學生/導師班

這個問題其實是兩個獨立成因疊在一起，都出在 `app/(app)/attendance/mobile/page.tsx`
（手機版「出缺勤登錄」）和 `app/(app)/attendance/weekly/page.tsx`（「一週出缺勤」）：

**根因 A：導師班/任課班查詢完全沒有依學年度篩選。**
系統累積超過一學年的資料後（現在已經有多個學年度的 `classes`／`class_schedule`
紀錄），同一位老師很可能在不同學年度都當過導師。原本查「自己導的班級」是：
```
supabase.from('classes').select('id').eq('homeroom_teacher_id', teacherRow.id).maybeSingle()
```
`.maybeSingle()` 只要查到 2 筆以上就會直接回傳錯誤（不是拋例外，是 `error`
欄位有值），而程式碼只解構了 `{ data }`，沒有檢查 `error`，所以 `data` 會是
`null`，接著會誤判成「沒有導師班」，掉進姓名比對的保險機制——保險機制一樣沒有
篩學年度，會抓到「同名教師在任一學年度」的任一筆班級（陣列裡第一個符合的），
不保證是今年的。「自己這天教的課」（`class_schedule` 查詢）同樣完全沒有篩
`academic_year`/`term`，會把過去學年度的排課也混進來，跟今天真正的課表對不上。

**修正**：兩個頁面都改成先呼叫 `resolveCurrentTerm()` 取得目前生效學年度，
「導師班」查詢加上 `.eq('academic_year', currentYear)`，`class_schedule`
查詢加上 `.eq('academic_year', ...).eq('term', ...)`；姓名比對的保險機制也一併
加上學年度篩選。同時修正了 `attendance/subject-view/page.tsx`（任課班級出席
查詢）同樣缺學年學期篩選的問題。

**根因 B：學生名單查詢沒有排除已轉班/畢業的舊紀錄。**
`enrollments` 表有 `is_current` 欄位標記「目前現行班級」（轉班/升級時舊紀錄會
被改成 `is_current=false`，但那筆紀錄的 `class_id` 還是指向原本的班級）。
`mobile`／`weekly` 兩頁原本查學生名單是：
```
supabase.from('enrollments').select('seat_no, student_no').eq('class_id', classId)
```
沒有加 `is_current=true`，導致已經轉出這個班的學生仍然會出現在每日出缺勤名單
裡（跟 `subject-view` 頁、`class-accounts` 頁原本就正確使用 `is_current` 的寫法
不一致）。

**修正**：兩頁的學生名單查詢都加上 `.eq('is_current', true)`。

## 3.「代課系統」現在完全無法進行代課安排

**根因**：跟第 1 點是同一套問題——`app/(app)/admin/substitute-teaching/page.tsx`
的 `load()` 也是直接呼叫 `supabase.rpc('current_academic_term')`，查無「目前
生效」時 `currentTerm` 一直是 `null`，而選節次、送出代課安排等操作全部被
`if (!leaveDate || !effectiveYear || !effectiveTerm) return;` 這類 guard 擋下，
表現就是「完全無法進行代課安排」。

**修正**：改用 `resolveCurrentTerm()`，邏輯同第 1 點。

**另外**：使用者要求「列出時間、請假人名、請假節次、請假理由、代課人選」——
查了一下畫面，「請假理由」欄位資料庫其實有存（`reason` 欄位，新增代課時的
表單也有輸入框），只是「近期代課紀錄」那張表格漏了這一欄沒有顯示出來。已經
補上「請假理由」欄（顯示在「請假教師」「代課教師」之後、「狀態」之前），
沒有理由的顯示「—」。

系統運行邏輯（`governed_tables` 白名單、RLS 政策、批次代課安排、自動通知）在
`sql/23academic_terms_and_substitute_teaching.sql`、`sql/31substitute_notifications.sql`
都已確認存在且沒有被改動，這輪只動到前端這兩個問題，沒有動資料庫結構，
**不需要額外執行新的 SQL 檔**。

## 4. 自聘教師也要放入「當年教師資料」，僅排除離職人員

**根因**：`lib/teacherLetters.ts` 裡 2026-08 那輪修正時，把需求理解成「當年
教師資料只收非自聘教師」，讓「自聘教師資料」跟「當年教師資料」變成完全互斥、
不重疊的兩群人（`listEligibleCertRows()` 對「當年教師聘書」多加了
`.eq('self_hired', false)` 的篩選）。這次確認需求是：**自聘教師也應該同時
出現在「自聘教師資料」與「當年教師資料」兩份名單，只有已離職的教師才要從
「當年教師資料」排除**，兩者不是互斥關係。

**修正**：`listEligibleCertRows('當年教師聘書')` 拿掉 `self_hired` 篩選，只保留
`.eq('resigned', false)`（未離職）；「自聘教師聘書」維持原樣，只看
`self_hired=true`，不排除離職（自聘那邊本來就有自己的離職欄位可以顯示）。
同時更新了範本表頭、批次上傳錯誤訊息、畫面上「歷年教師資料目前沒有 ×× 的
教師」提示文字，把所有還在講「非自聘」的地方改成「未離職（含自聘）」，避免
文字誤導使用者。

這個改動不需要資料庫遷移——`teacher_service_certificates`／
`teacher_appointment_letters` 兩張表結構完全沒變，純粹是查詢條件跟畫面文字
的調整。已經在資料庫裡的資料不需要清洗或搬移。

## 這輪修改的檔案清單

- 新增 `lib/academicTerm.ts`（共用的 `resolveCurrentTerm()`）
- `app/(app)/schedule-lookup/page.tsx`
- `app/(app)/attendance/mobile/page.tsx`
- `app/(app)/attendance/weekly/page.tsx`
- `app/(app)/attendance/subject-view/page.tsx`
- `app/(app)/admin/substitute-teaching/page.tsx`
- `lib/teacherLetters.ts`
- `components/dev-tools/TeacherLettersPanel.tsx`

沒有新增/修改任何 SQL 檔案，這輪全部是前端查詢邏輯與畫面文字的修正，
**直接部署新的程式碼即可，不需要額外跑 SQL Editor**。

## 建議：把「學年學期設定」設為目前生效，避免類似問題再發生

第 1、3 點的根因說明了一個共通的操作風險：只要 `academic_terms` 沒有任何一筆
被標記「目前生效」，愈來愈多依賴 `current_academic_term()` 的頁面就會用到
`resolveCurrentTerm()` 的「猜測退回值」，雖然這樣頁面不會再整個打不開，但猜的
畢竟不一定是管理者真正想要的那個學年學期。建議請開發人員（dev 部門帳號）到
`/admin/academic-terms` 頁面，確認現在（115 學年度）有正確設定「設為目前生效」，
之後每學期開始/結束時也記得手動切換，這樣所有依賴這個值的頁面都會用到正確、
明確設定過的學年學期，而不是退回猜測值。
