# 2026-08-17 追加修正（第十輪）：學業平均查詢漏欄位、出缺席科目被誤排除、評語外鍵錯誤、操行等第/合併儲存格

延續第九輪。安裝順序：這輪**沒有新增 sql 檔**，只有改 `sql/48fix_attendance_score_formula.sql`
本身（幫 subject_scores 的出缺席那段加一個防呆，避免「全勤」「出缺席」兩個名稱都設定時
被算兩次）——如果你已經執行過原本的 sql/48，請重新執行一次這個檔案覆蓋過去即可
（裡面都是 `create or replace`，可以重複執行）。其餘都是前端／程式邏輯修改。

## 1. 出缺席正負號／平均不降反升

追查後在 `lib/reportCard.ts` 抓到兩個實際的程式錯誤（不是資料庫公式錯，是我這兩輪
自己漏改的地方）：

1. **查詢 `student_examtype_totals` 時，根本沒有把 `midterm_average`／`final_average`／
   `daily_average` 這三個欄位加進 `select` 裡**，只查了 `midterm_total` 等三個「直接
   加總」的欄位。這代表不管資料庫算得對不對，成績單上「學業平均」期中/期末/平時那
   三格程式碼永遠讀不到值——這也直接對應這輪你反映「學業平均期中/期末/平時分數沒
   顯示」，是同一個根因。
2. `visibleSubjects`（決定成績單科目列表要印哪些科目）裡还留著上一輪(sql/46)時代
   「全勤/出缺席不能出現在科目列表，會重複計算」的排除條件——但 sql/48 已經把出缺席
   改成正常科目計算方式，這個排除條件變成「檔案裡另外又寫死了一列出缺席的
   attendanceScore、真正的科目清單卻又把它排除掉」，兩邊各自為政，容易對不起來，
   也是造成「出缺席%沒顯示」的原因。已經把排除條件拿掉，並且把 `ReportCardDocument.tsx`
   裡原本另外寫死的那一整列「出缺席」也拿掉了（不然會變成印兩次重複的出缺席列）——
   現在出缺席就是科目清單裡的其中一列，比重％會正常顯示。

**這兩個修正之後，用你這次給的數字反推：** 蔡智強原本總分552、平均73.5（出缺席還沒
算進去時）；出缺席這學期扣分加總後的原始分數（不是100分為底、是直接的扣分點數
加總）應該是 -43，直接加總的「總分」欄 552-43=509（你確認這個是對的）；「平均」
那一欄的公式＝其他科目已經算好的加權平均，加上「出缺席分數 × 出缺席科目比重
（3%）」＝ 73.5 + (-43 × 0.03) ＝ 73.5 - 1.29 ≈ **72.21**，跟你認為正確的72.2幾乎一樣。
換句話說，**公式本身的正負號是對的**，「平均反而升高」這個現象比較可能是套用到上面
那兩個實際程式漏洞（沒查到 average 欄位、出缺席被排除在外）綜合造成的顯示異常，
而不是資料庫公式本身正負號顛倒。麻煩這次程式碼更新後**重新測一次**，如果平均數字
還是不對，麻煩把下面這兩組數字直接告訴我（不用截圖也可以）：
- 「科目與比重設定」頁，高三年級、出缺席（或全勤）那一列目前的比重數字是多少；
- 「整體佔比與加扣分規則」頁，曠課/遲到/病假/事假/公假目前各自設定的點數是多少；

有這兩組數字我才能精確核對是不是還有沒抓到的地方，而不是繼續用猜的改。

## 2. 評語儲存失敗（外鍵錯誤）

**根因**：我上一輪寫的存檔程式碼把 `getCurrentAppUser().id`（登入帳號本身在
`app_users` 表的 id）直接存進 `student_remarks.updated_by`——但這個欄位參照的是
`teachers` 表的 id，兩張表的 id 不是同一個值，資料庫當然擋下來。系統裡「通知」頁
之前也遇過同樣的問題，當時新增了一支專門轉換用的函式 `getCurrentTeacherId()`
（沒有連結教師身分的純管理帳號會拿到空值，一樣可以存檔，只是不記錄是哪個教師）。
這裡改用同一支函式，已修正。

## 3. 成績單樣式：學業平均/出缺席%沒顯示、沒有操行等第、日期欄位、操行比重格合併

- **學業平均、出缺席%沒顯示**：跟第1點是同一個程式漏洞，已一併修正（見上面說明）。
- **日期欄位**：這個其實原本就有做（`lib/ReportCardDocument.tsx` 右側直式的
  「二〇二六年八月十五日」那一條，用中文數字），如果你看到的版本沒有，可能是還沒套用
  到最新的程式碼；這次的壓縮檔裡有，麻煩重新整包部署後再確認一次。
- **操行等第**（甲/乙/丙/丁大字）：原本完全沒有做這個功能，這輪新增了：依「操行成績」
  分數換算成等第，換算級距我是**用你截圖裡「操行成績65分＝丙」反推**出一組級距
  （90分以上甲、80-89乙、60-79丙、60分以下丁），**還沒有跟你確認這是不是貴校實際
  的規則**，如果級距不對，這是一個獨立的小函式（`conductGradeLabel()`），我可以馬上
  按你告訴我的正確級距調整。
- **操行比重格合併**：已經把操行成績/禮貌/衣著/服務/紀律這五列的「比重」欄，改成
  合併成一個貫穿五列的大格子（等第欄也一樣，用同一個合併大格子放甲/乙/丙/丁），
  版面上應該會更接近你的樣本。

**這部分我要老實說明一個限制**：`react-pdf`（用來產生 PDF 成績單的套件）不像
Word/Excel 那樣可以直接預覽排版，我這邊沒辦法像操作 Excel 一樣「畫出來看一眼對不
對」，只能照著程式邏輯推算應該長什麼樣子，**這次的版面調整沒有實際印出來目視確認
過**，麻煩你套用後實際列印/預覽一次，如果合併儲存格的位置、寬度、對齊有跑掉，
把印出來的截圖給我，我可以照著實際落差再微調，會比我這邊憑空猜測有效率很多。

如果你評估後覺得每次都要「印出來、截圖、我再猜、再印」這樣來回效率太低，之前
（第八輪）提過的方向還是可以考慮：另外做一個瀏覽器端可以直接預覽、甚至拖曳調整
位置的成績單編輯器，這樣你自己就能即時看到調整結果，不用每次都靠文字描述落差。

## 這輪還沒處理的部分

- 出缺席正負號如果套用這輪修正後還有問題，需要你提供第1點列的兩組實際數字才能
  精確定位。
- 操行等第的級距需要你確認是否正確。
- `attendance_adjustment()` 這支舊函式（sql/46 時代的公式）目前確認已經沒有任何地方
  在呼叫，下次如果要順便清理可以直接 `drop function`，這輪先保留沒動它。
