# 2026-08-16 追加修正（第八輪）：期中/期末/平時個人列印、訓導出缺勤權限、排名依平均、出缺席扣分可編輯

這一輪對應你這次提出的六個反映事項（原始編號沿用），安裝順序：先套用
`sql/47ranking_average_discipline_access_partial_report_card.sql`（需在 `sql/1schema.sql`
到 `sql/46wire_attendance_and_discipline_adjustments.sql` 全部執行過後再執行），
其餘都是前端／程式邏輯修改，跟著整包程式碼一起部署即可。

## 1. 「期中」「期末」「平時」沒有個人列印，只有「全部」能印成績單＋下載Excel

**根因**：`lib/reportCard.ts` 原本用 `report_card_ready()` 判斷「能不能產出成績單」，
這支函式要求期中考／期末考／平時分**三項全部鎖定**才回傳 true，任何一項還沒鎖，
整份成績單（含已經鎖定的部分）就完全印不出來、也不會顯示任何已完成的數字。

**修正**：
- 新增 `report_card_any_locked()`（三項「至少有一項」鎖定即可）、
  `report_card_exam_type_locks()`（一次查三項各自的鎖定狀態），
  `getReportCardResult()` 改用前者當作「能不能先產出」的門檻，`buildTermBlock()`
  改成三項分開判斷：已鎖定的欄位正常顯示數字，還沒鎖定的欄位維持空白——不再
  「一項沒鎖，全部都印不出來」。
- 需要三項合併才有意義的欄位（學業平均「總分」欄、全班排名）維持只在三項都
  鎖定時才顯示，避免印出還會變動的排名，容易造成誤會。
- 【班級成績總表】頁新增「📊 下載成績 Excel」按鈕（`lib/excelTemplates.ts` 新增
  `downloadClassScoreExcel()`），直接把畫面上正在顯示的科目/總分/平均/班排/年排
  資料整理成一份 Excel 下載，檔名依班級/學年學期/目前檢視的階段（期中/期末/平時/
  全部）命名，不用另外查一次資料庫。

## 2. 訓導主任看不到全校出缺席紀錄，被說「資格不足」

**根因**：兩層問題疊在一起——
1. `attendance` 表的 RLS 政策只開放給管理員／該班導師／該節任課教師，訓導部門
   （不管承辦人員還是主管）完全不在裡面，查詢會被擋掉大部分列。
2. `app/(app)/reports/school-attendance/page.tsx` 只用 `isAdminInCurrentView()`
   判斷能不能看整頁，一樣只認得到 admin_a/admin_b/system_admin_s。

**修正**：新增 `discipline_dept_read_attendance` RLS 政策（`has_department('discipline')`
即可讀取全校 `attendance`），連帶讓依賴 `attendance` 的 `student_absence_counts` view
自動生效（該 view 本來就是 `security_invoker=true`）；另外新增
`attendance_notifications` 的訓導讀取政策，讓訓導部門也看得到導師的處理紀錄。頁面
權限判斷改成「管理員 或 訓導部門成員」都能看，並保留原本「切換身分」邏輯（管理員
切到教師視角時一樣視為不可見）。

## 3. 成績單版面拖曳調整／修正建議

這輪**沒有**實作真正的拖曳版面編輯器——那是一個獨立的大功能（需要一個所見即所得的
版面定位工具，跟現有「成績單樣式設定」頁的顏色/字級/文字標籤調整是不同量級的工程），
這次時間內沒有做。

**目前你可以做的替代方案**：「成績單樣式設定」頁已經可以調整顏色（7項）、字級/邊框
粗細（4項）、23個標題文字標籤，不需要改程式碼就能讓成績單「看起來」比較接近你要的
樣子。如果跟你上傳的樣本不一樣，麻煩告訴我**具體是哪些欄位的位置/大小/順序不對**
（例如「操行成績那一列應該在出缺席上面」「科目欄位太窄，长科目名稱會被截斷」這種
具體描述，最好附上你覺得不對的截圖跟對應的樣本截圖），下一輪我可以針對性調整
`lib/ReportCardDocument.tsx` 的版面（改程式碼本身，不是每次都要重新開發拖曳工具）；
如果你評估過後還是需要一個真正的拖曳編輯器（管理者自己在瀏覽器裡拖動每個欄位的
位置/大小、存成設定檔），這是可以做的，但建議另外排一輪專門處理，會牽涉：
1. 把 `ReportCardDocument.tsx` 現在「寫死的版面」拆成「每個區塊都有可調整的
   x/y/width/height」的資料結構；
2. 一個瀏覽器端的拖曳畫布（用滑鼠拖動/縮放每個區塊，即時預覽）；
3. 存成 JSON 設定檔（可以沿用現有「成績單樣式設定」頁的下載/上傳機制擴充）。

## 4. 排名應該依「平均」排序，避免總分高但比重換算後名次錯誤

**根因**：這是一個延續上一輪(`sql/45`)遺留的 bug——`sql/45swap_total_and_average_formulas.sql`
把 `student_examtype_totals` 的欄位公式對調過來（`_total`＝各科分數直接加總、
`_average`＝依比重加權平均），但 `sql/44` 建立的排名函式/view
（`class_rankings`／`grade_rankings`／`class_rankings_for_class()`／
`grade_rankings_for_class()`）裡，期中/期末/平時各自的班排名／年級排名，
`rank() over (order by ..._total desc)` 沒有跟著換，結果變成「用各科分數直接加總」
去排序——選修科目較多、或剛好選到比重高科目拿高分的學生，加總數字比較大，換算成
加權平均後不一定真的表現最好，名次卻排在前面，這正是這次反映「總分高但比重換算後
產生名次錯誤」的根因。

**修正**：這四個排名函式/view 的 `order by` 全部改成看 `_average`（依比重加權平均）。
畫面上「總分」那一欄顯示的數字不會變（那是給你對照原始加總用的），只有「班排名」
「年級排名」欄位的名次可能會因為這個修正而改變——如果你手上有先前用真實資料驗證過
的排名結果，這次套用後名次可能會有變動，屬於預期中的修正，不是新 bug。

## 5. 學業平均的期中、期末、平時分數未出現

跟第 1 點是同一個根因（`report_card_ready()` 要求三項全部鎖定才顯示任何學業平均），
已經隨第 1 點的修正一起解決：現在期中/期末/平時三欄各自只看「自己」有沒有鎖定，
鎖定了就會顯示對應的加權平均分數，不用等其他兩項一起鎖定。

## 6. 出缺席%找不到在哪輸入（固定3%）＋事假/曠課/缺席倒扣分沒有反映（高三忠1號都是100）

追查後發現這裡其實是**兩個獨立問題**疊加：

**(a) 倒扣分點數（`conduct_point_defaults`：曠課-0.1、遲到-0.02、事假-0.05…）
從來沒有地方可以編輯**——「整體佔比與加扣分規則」頁（`GradingRulesTab.tsx`）
這張表原本是純文字唯讀顯示，而且說明文字還寫著「僅供參考記錄，目前不會自動套用到
總成績」，這句話是上一輪把它接上真正的計算邏輯（`sql/46`）**之前**留下的舊文字，
沒有跟著更新——等於同時「無法編輯」又「文字說明還告訴你這功能沒有作用」，難怪找不到
在哪裡調整。已修正：這張表改成可以直接輸入新的點數存檔（只有系統管理員或訓導部門
主管能存，這是資料庫既有的權限規則，這裡讓畫面上的權限判斷跟資料庫規則一致），
說明文字也改成正確描述（這些點數會自動套用到出缺席分數/操行成績）。

**(b) 「全勤」／「出缺席」科目比重（也就是「出缺席%」，例如3%）本身是可以編輯的**——
它其實就是「科目與比重設定」頁裡一個名稱叫「全勤」或「出缺席」的一般科目列，只是
因為它跟「整體佔比與加扣分規則」是兩個不同頁面，你在後者裡找不到，才會覺得像是
「固定3%改不了」。已在「整體佔比與加扣分規則」頁新增一個唯讀對照表，列出目前資料庫
裡各年級「全勤／出缺席」科目的比重值，並註明要修改請到「科目與比重設定」頁操作，
减少兩個頁面互相找不到的問題。

**(c) 「高三忠1號分數欄位都是100」**：這不是扣分沒有生效，是【班級成績總表】／
【成績登錄】頁的「全勤」科目欄位，一直顯示的是老師手動輸入、但實際上**不會被排名/
總分採用**的舊分數（100分）——真正算出來的出缺席分數（含扣分）只有在正式列印的
成績單上看得到，兩個畫面數字對不起來，容易讓人誤會扣分沒有生效。已修正：
【班級成績總表】頁「全勤」／「出缺席」欄位改成直接顯示系統依真實出缺勤紀錄自動
算出來的分數（新增 `class_attendance_adjustment_batch()` 資料庫函式，一次查完全班，
不用每個學生各自呼叫一次），滑鼠移到格子上會有說明；【成績登錄】頁選到這個科目時
也會跳出提示，說明這裡輸入的分數不會被採用、真正的計算結果要去哪裡看、要調整扣分
點數要去哪一頁改。

## 這輪還沒處理、需要你確認或另外排時間的部分

1. **第 3 點的拖曳版面編輯器**：如上面說明，需要你先給具體的版面落差描述，或確認
   要不要另外排一輪做真正的拖曳工具。
2. **建議實際測試**：這次沒有連線到你的正式資料庫，`sql/47...sql` 套用後建議先在
   測試環境（或先找一兩個班）驗證：(a) 期中考單獨鎖定後，個人成績單能不能正常印出
   只有期中欄位的部分；(b) 訓導主任帳號登入後，「全校出缺席狀況總覽」能不能看到
   資料；(c) 排名名次套用新公式後，跟你手上原本核對過的名次相比，差異是否合理。
3. 上一輪（第七輪）遺留、你還沒回覆的兩個問題（全勤科目比重要不要順便改成0、
   出缺席顯示邏輯的上下限細節）這輪沒有變動，維持原樣等你確認。
