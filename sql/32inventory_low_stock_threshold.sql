-- ============================================================
-- 32：校服／簿本庫存販賣表改版所需的資料庫調整
-- ------------------------------------------------------------
-- 使用者提供的參考原型（總.zip 的 v9.html）多了「庫存警戒值」這個原本
-- general_inventory_items 沒有的欄位：低於這個數字時，畫面上品項名稱要
-- 放大兩倍並改紅字提醒（沿用同一份規則套用到「書庫」的書籍也不衝突，
-- 只是書庫頁面目前沒有用到這個欄位，先加上以後如果要沿用也不用再改資料庫）。
-- 沒有這欄位以前的舊資料，一律先給預設值 3，之後可以在畫面上個別調整。
-- ============================================================

alter table general_inventory_items
  add column if not exists low_stock_threshold int not null default 3 check (low_stock_threshold >= 0);
