# 交接文件 2026-08-11（第二輪）：備份筆數短少／任課教師無法登錄出缺勤／通知變個人化

延續同一天稍早那輪（`HANDOVER_2026-08-11.md`）之後，這輪處理 4 個新回報的問題。
已跑過 `npx tsc --noEmit` 確認沒有新增的型別錯誤（`app/api/reports/report-card/`
底下那 2 個既有錯誤跟這輪修改的檔案完全無關）。

## 1. 管理人S一鍵下載備份，學生只有1000人、成績只有2000筆

**根因**：`lib/backupRestore.ts` 的 `runBackup()` 原本每張表只發一次
`admin.from(table).select('*')`，沒有用 `.range()` 分頁。Supabase 的 API 層
（PostgREST）對「沒有明確指定 range」的查詢會套用伺服器端設定的單次回傳上限，
超過上限的資料會被**靜默截斷**——不會回傳錯誤，`data` 就只有前面那一批，備份
檔案看起來成功、實際筆數卻遠低於真正的資料量，等於做了一份不完整、不能拿來
真正復原的備份（這也是「每天自動備份」`app/api/cron/daily-backup/route.ts`
共用的同一支函式，所以自動備份也有一樣的問題）。

**修正**：改成 `.range()` 依 1000 筆一頁分頁撈到底（撈到某一頁回傳筆數小於
1000 才停止），不管伺服器單次上限實際是多少，都能確保撈到全部資料。手動備份
（`/api/admin/backup/create`）、每日自動備份（`/api/cron/daily-backup`）、
一鍵清空前的安全備份（`/api/admin/wipe-term-data`）都是呼叫同一支
`runBackup()`，這次改完三個路徑一起修好，不用個別調整。

**建議**：下次「還原」之前，先用「下載備份」把 JSON 存下來確認筆數合理（例如
跟學生人數、成績筆數的既有認知對一下），確保這份備份確實完整再放心依賴它。

## 2. 學生出缺席登錄只有導師登錄功能，任課教師無法登錄

**根因**：`app/(app)/attendance/mobile/page.tsx` 原本有兩個 `useEffect`：
第一個會依 `class_schedule` 查出「自己今天教的課」設進 `periods`；但只要這位
老師「同時是導師」，第二個 `useEffect` 就會整個覆蓋 `periods`，換成依
`period_config` 算出來的一份通用清單（全部標成「導師登錄」）——導師自己任課的
節次因此完全消失，沒辦法在這頁登錄自己教的課。另外，單純的任課教師（不是
導師）雖然看得到自己的課，但畫面上所有節次共用同一個全域 `classId`，只會鎖死
在「當天課表第一筆」的班級——如果同一天教兩個不同班級，點第二個節次時
`classId` 不會跟著換，會顯示錯的班級名單，等於實質上也沒辦法正確登錄。

**修正（照您建議的兩色設計做）**：改成兩份清單合併顯示、不互相覆蓋，每個節次
按鈕各自帶自己的 `classId`（不再共用一個全域的）：
- 深色（黑）：自己今天有授課的節次，來自 `class_schedule`，可能橫跨不同班級，
  按鈕上會顯示是哪個班（例如「第3節・1年甲班」）。
- 綠色：如果自己是導師，導師班「當天全部節次」（不論是不是自己教的），供導師
  查詢/編輯整班的出缺勤。跟深色重複的節次（同一節、同一個班本來就是自己導師班）
  不會重複顯示。
- 補登範圍（1週內免申請）跟鎖定放寬規則，原本是「只要我是任何一個班的導師」
  就整頁放寬，改成只有在編輯「自己導師班那個節次」時才放寬——編輯自己任課、
  但不是自己導師班的節次，還是照一般任課教師規則走（避免導師身分被誤用來
  繞過別班的鎖定/補登限制）。
- 選日期補登時，會依新選的日期重新算「星期幾」對應的節次清單（原本這段邏輯
  只在剛進頁面時執行一次，換日期不會重算）。

任課教師本來就有寫入權限（`sql/22department_policy_rewrite_complete.sql` 裡
`can_write_attendance()` 已經允許 `class_schedule.teacher_id = 自己` 的節次
寫入），這次沒有動到資料庫政策，純粹是前端把節次清單/班級對應的邏輯改對。

## 3.「學年學期設定」之前設定過，剛剛看沒有了

這輪特別去查了一遍會不會有程式碼把 `academic_terms.is_current` 意外重置掉：
`set_current_academic_term()`（切換目前生效）、`handleAdd()`（新增學年學期）、
`一鍵清空`（`wipe-term-data`，確認過沒有動 `academic_terms` 這張表）都檢查過，
沒有找到會把 `is_current` 靜默清掉的程式碼路徑。比較合理的解釋是：`academic_terms`
也在完整備份/還原的資料表清單裡（`lib/backupRestore.ts`），如果中間有人執行過
「還原」到一份較舊的備份，會連帶把 `is_current` 還原回那個時間點的狀態——但這是
「還原」功能本來就會做的事，不是 bug。既然您已經重新設定過，這裡就不用再處理；
如果之後又發生類似「設定消失」的情形，麻煩留意一下當時是否剛好執行過「還原」，
會比較好追根因。

第一輪（`HANDOVER_2026-08-11.md` 第1、3點）已經把「查詢教師/班級課表」「代課
系統」這兩個原本會因為找不到『目前生效學年學期』就整頁打不開的地方，改成查無
資料時會自動退回最合理的猜測值（`lib/academicTerm.ts` 的 `resolveCurrentTerm()`），
所以就算之後又發生類似「設定消失」的狀況，這兩頁也不會再整個打不開。

## 4. 通知目前為所有人共用，請變成個人（相關人員）提示

**根因**：`staff_notifications` 這張表本來就有 `teacher_id` 欄位、資料庫 RLS
政策也已經限制一般老師只能讀到 `teacher_id = 自己` 的那幾筆——但政策裡刻意放寬
「系統管理員／訓導部門可以看到全校所有教師的通知」（原意是給審核/監督用），
而「通知」頁（`app/(app)/notifications/page.tsx`）跟頂端導覽列的未讀角標
（`components/TopNav.tsx`）兩處查詢都完全沒有自己在前端加上 `teacher_id`
篩選、直接依賴 RLS 幫忙擋。結果系統管理員（S）這類帳號打開「通知」頁，看到的
就是全校所有老師的通知混在一起、角標也是全校未讀總數，感覺像是「大家共用
同一份」。

**修正**：新增共用函式 `getCurrentTeacherId()`（`lib/supabaseClient.ts`），
「通知」頁跟 TopNav 角標都改成明確查詢自己的 `teacher_id`，只抓屬於自己的
通知/未讀數——不再依賴 RLS 的審核用放寬條件。沒有連結教師資料的純管理帳號
（沒有 `teachers` 資料列）會顯示「沒有屬於自己的個人通知」，角標維持 0，而不是
退回顯示全校通知。

**注意（範圍說明，沒有動）**：資料庫 RLS 政策本身沒有改——系統管理員／訓導
部門仍然「有能力」讀到全部教師的通知（這個放寬是特地為了審核/監督保留的，
資料庫層面拿掉的話會影響其他還沒探勘到的既有用途）。如果之後真的需要一個
「管理員審核用、可以看到全校通知」的畫面，建議另外開一個獨立頁面（例如
「通知審核總覽」），跟這裡的「個人通知」分開，不要合併在同一個查詢或同一頁
畫面裡，才不會又退回「看起來像共用」的狀態。

## 這輪修改的檔案清單

- `lib/backupRestore.ts`（備份分頁修正）
- `app/(app)/attendance/mobile/page.tsx`（重寫：任課/導師節次合併顯示）
- `lib/supabaseClient.ts`（新增 `getCurrentTeacherId()`）
- `app/(app)/notifications/page.tsx`（改成查自己的 teacher_id）
- `components/TopNav.tsx`（未讀角標改成查自己的 teacher_id）

沒有新增/修改任何 SQL 檔案，這輪全部是前端查詢邏輯的修正，直接部署新的程式碼
即可，不需要額外跑 SQL Editor。
