# 2026-08-22 追加修正（第二十二輪）：終於能親眼看見PDF長怎樣了、外頁換成真實圖片、內頁三處修正

這輪有個工具面的重要突破，先說一下：這個環境裡其實一直有 `pdftoppm`（PDF轉圖片
的工具），我前幾輪一直只用「把PDF裡的文字抓出來核對數字」的方式驗證，卻沒有
真的把PDF轉成圖片用眼睛看過排版——**這次是我第一次真的「看到」PDF長什麼樣子**，
不是用猜的。之後版面類的修改都會用這個方式驗證，會準確很多。

## 1. 外頁重新製作（用你附的「外頁修正.xlsx」）

這份檔案裡**藏著兩張真正的圖片**（校徽、校園照片）——之前一直是色塊佔位，這次
把圖片直接從你的 Excel 檔案裡抽出來，放進系統當內建預設圖（`public/images/
school-logo.png`／`campus-photo.jpg`），不用再上傳，成績單會直接使用真的校徽跟
校園照片。如果你在【成績單樣式設定】頁另外上傳了圖片，會優先用你上傳的；沒上傳
就用這兩張真的圖。

同時發現這份檔案裡「操行成績標準」右邊的區塊，內容順序其實跟前幾輪不一樣：
**出缺席加扣分排第二（不是獎懲）、學業佔比排第三、特殊表現(獎懲)排第四**——已經
照這個順序重排。

這次也是第一次真的截圖看過輸出結果（見上面說明），確認校徽、校園照片、文字位置
排版基本上跟你的樣本一致。

## 2. 操行5行分數/等第都改成兩格

已經用截圖確認過，跟上一輪（round21）修的結果一致（分數2格、等第2格，各自對齊
上下學期），如果你這次反映的落差是根據更早之前（round20）的版本看到的，這次
應該已經是對的，麻煩重新確認一次最新的PDF。

## 3. 全勤記錄上移到公假下方

已修正。原本全勤是另外獨立一列（排在曠課/遲到/病假/事假/公假五列+一個空白列
之後，等於第7列），已經改成直接接在公假後面（第6列），跟懲獎那邊的6個項目
（嘉獎/小功/大功/警告/小過/大過）對齊。

## 4. 學業平均跟隔壁欄合併

已修正——照你的樣本檔案，「學業平均」這個標籤原本應該直接佔滿「科目+比重」
兩欄合起來的寬度（不是像其他科目一樣分開兩欄），前幾輪這裡漏改了，這次補上，
跟操行成績那五列現在是同樣的處理方式。

## 順手抓到一個小bug：「忠班班」重複

這次截圖檢查排版時意外看到內頁跟外頁的班級名稱都印成「高三忠班班」（多一個
「班」字）——系統裡「班級名稱」這個欄位存的值本身就已經包含「班」字（例如
"忠班"），成績單版型又另外加了一個「班」字，變成重複。已經拿掉版型裡多加的
那個字，改成「高三忠班」。

## 技術細節：為什麼要另外處理圖片路徑

這次把真實圖片路徑寫在成績單的排版檔案裡時，原本直接用了 Node.js 的 `path`
這個模組組路徑——但這個排版檔案（`lib/ReportCardDocument.tsx`）也會被瀏覽器端
的【成績單樣式設定】頁引用到（借用裡面定義的資料格式），如果直接在這個檔案裡
用 `path`，瀏覽器端的頁面會建置失敗。已經把「組圖片路徑」這件事移到專門給伺服器
API用的檔案（`lib/reportCard.ts`）處理，排版檔案本身維持乾淨、不含任何只有
Node.js 才有的模組，兩邊都不會壞。

**部署時記得把 `public/images` 這個新資料夾也一起上傳**，裡面是這次新增的校徽/
校園照片檔案，沒帶到的話圖片會讀不到（會退回顯示色塊佔位，不會整份壞掉，但
就看不到真的圖）。
