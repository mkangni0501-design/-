# 2026-08-19 追加修正（第十四輪）：排名查詢逾時的真正根因、家長/學生登入驗證開關

安裝順序：`sql/50fix_ranking_query_timeout.sql` 需在 sql/49 之後執行；
`sql/51portal_login_verification_toggle.sql` 可以獨立執行，不依賴 sql/50。

## 2.（優先處理）總分/排名查詢逾時——這應該就是連續好幾輪「管理員S看不到排名」的真正根因

上一輪新增的錯誤訊息顯示機制這次立刻發揮作用：`canceling statement due to
statement timeout` 這個訊息代表**查詢真的跑到一半被資料庫強制中斷**，不是「查得到
但被權限擋掉」——這代表前幾輪我一直往「權限判斷」的方向排查，方向可能一直是錯的，
真正的問題是**效能**。

**根因**：上上輪（sql/48）把「出缺席」改成透過 `subject_scores` 這個共用資料表
（view）計算時，那段新增的程式完全沒有限制「只算某個班/某些學生」，而是「不管
外面只需要哪幾個學生的資料，都先把全校 1300多位學生的出缺席分數通通算一遍」
（而且期中/期末/平時三個欄位還各自重複算一次，等於一位學生要算3次）——加總起來
要跑好幾千次計算，才會在查詢「一個班」的總分排名時，被資料庫的逾時保護機制直接
攔腰砍斷。之前幾輪查了半天「S的權限」，方向其實找錯了，很抱歉繞了這麼多輪才抓到。

**修正**：把「總分/排名」查詢改成一開始就先鎖定「這個班」或「這個年級同部別」的
學生名單，才開始計算出缺席分數/科目加權——確保資料庫從第一步就不會去碰這份名單
以外的學生，不會再有「查一個班卻要算全校」的狀況。出缺席分數本身的計算也從
「查兩次」合併成「查一次」，再省一半的成本。

這應該會同時解決「管理員S看不到總分排名」跟連帶影響到的「批次列印/個人列印」（如果
它們的緩慢/失敗也是同一個逾時問題）。**麻煩部署後實際測一次**，如果還有同樣的
逾時訊息，畫面上會直接顯示出來，把訊息內容給我，這樣才能確定是不是還有其他地方
也有一樣的全校掃描問題（例如成績單單一學生列印那條路徑，這輪還沒有動，理論上
影響範圍小很多，但如果之後也發現慢，可以用同一招處理）。

## 3. 家長／學生查詢入口：新增「是否啟用信箱驗證」開關

已在【開發人員區】頁面最下面新增「家長／學生查詢入口設定」區塊，一個勾選框：
- 勾選（預設）：維持原本「輸入代碼＋信箱 → 寄驗證信 → 點連結登入」的流程。
- 取消勾選：輸入代碼＋信箱、按「登入」後**直接進入查詢頁**，不會寄信，也不需要
  點任何連結。

技術上：關閉驗證時，伺服器端會用 Supabase 官方提供、給後台系統直接核發登入憑證
用的正規機制（不寄信、不繞過安全機制的土砲寫法）直接建立一個已登入的 session
回傳給瀏覽器，跟原本「等信箱驗證完成」拿到的 session 本質上是同一種東西，只是
省略了寄信/點連結那個步驟。

**之後恢復對外連線，記得回來把這個開關重新勾選**——關閉期間，任何人只要知道正確
的「登入代碼＋登記信箱」就能直接登入，不需要真的收得到那個信箱的信，畫面上也有
提示文字說明這件事。

## 1. 版面轉換

這輪把時間集中處理上面這個「查詢逾時」的根因排查與修正（這個問題已經連續影響
好幾輪的功能，優先度必須拉到最高），加上新增登入開關這個功能，版面轉換這輪還是
沒有進展，很抱歉。下一輪如果排名/列印的問題確認解決了，應該可以比較專心處理版面。
