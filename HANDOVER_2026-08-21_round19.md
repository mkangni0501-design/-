# 2026-08-21 追加修正（第十九輪）：抓到全勤%乘了兩次100的公式bug、外頁改回橫式並補上泰文

這輪沒有 sql 檔案，全部是 `lib/reportCard.ts`／`lib/ReportCardDocument.tsx`／
`lib/pdfFonts.ts` 的修改，加上兩個新的泰文字型檔案（`public/fonts/NotoSansThai-
Regular.ttf`／`NotoSansThai-Bold.ttf`，整包程式碼裡已經有了，部署時記得帶著
public/fonts 這個資料夾一起上傳，不然泰文字會印不出來或報字型錯誤）。

## 1. 全勤/曠課/遲到/事假/病假/公假 的 % 都印錯

**抓到了，是我自己的公式寫錯**：`lib/reportCard.ts` 算「換算成最終佔比後的
影響」這個百分比時，多乘了一次100——原始分數乘上比重（例如全勤100分×3%比重＝3）
這一步算出來的「3」本身就已經是「3%」的意思了，我卻又把它乘了一次100，變成
「300%」。曠課/遲到/事假這幾項因為原始數字本身很小（-3.33、-0.67、-1.67），
乘錯之後看起來還是很小的數字（-333%看起來像打字打錯，不容易一眼看出問題），
只有「全勤」這種原始分數比較大（100分）的項目，乘錯後的「300%」才明顯到一眼
就能看出不對——這也是為什麼前幾輪我自己測試時沒抓到，因為我當時是直接手動輸入
「應該要對」的數字去測 PDF 排版，沒有真的走一次「從資料庫算出百分比」這個步驟。
已經修正，這次重新核對過：全勤+3.00%、曠課-0.10%、遲到-0.02%、事假-0.05%、
病假-0.00%、公假-0.00%（病假/公假雖然是0，比照你附的原始文件寫法，印成
「-0.00%」帶負號，不是「+0.00%」）。

## 1（續）. 外頁改回橫式，補上泰文

- 方向改回橫式（A4 landscape），跟內頁一致。
- 泰文這次真的補上了，不是文字說明、是實際印在PDF上的泰文字：另外找了一套
  支援泰文的字型（Noto Sans Thai），跟中文字型（Noto Sans TC）分開註冊、各自
  用在對應的文字上——上一輪拿掉泰文是因為中文字型本身不含泰文字符，直接印會是
  亂碼，這次是真的解決了字型問題，不是繞過去。這次一樣實際跑了PDF產生、把文字
  抓出來確認泰文字元正確無誤（不是亂碼），四行泰文（標題、校名×3行）都在。

## 2. 內頁版面

這次沒有再另外調整內頁版面結構本身（上一輪已經照你提供的樣本圖調整過一次，
補上了全學年欄位、雙等第等），這輪主要是配合外頁的字型/方向調整，內頁維持不變。

**關於「如仍不能跟參考圖片一樣，請製作成可由我自行調整表格的模板」**——這句話我
想先跟你確認清楚方向，再決定怎麼做比較有效率：目前【成績單樣式設定】頁已經可以
調整顏色、字級、邊框粗細、文字標籤這些「樣式」，但沒辦法調整「版面配置」（例如
某個欄位的位置、寬度、要不要顯示）。做一個「可以讓你自己拖曳調整表格版面」的
編輯器是可行的，但是一個獨立的中型功能（瀏覽器裡即時預覽＋拖曳/縮放每個區塊＋
存成設定檔套用到所有成績單），跟這幾輪「我改程式碼、你列印看效果、回饋落差」
的模式是不同的做法，需要另外排一輪專門做。

在那之前，如果現在的版面还有具體落差，其實**直接告訴我哪個欄位/區塊不對**
（例如「操行成績那個區塊要往左移」「全學年欄位太窄」）still是最快的方式——這幾輪
下來，我發現只要有你的實際樣本圖或明確的文字描述，通常一兩輪就能對齊；比較容易
卡住的反而是「有沒有真的測試過」（例如這次的%乘兩次100，是因為我自己手動輸入
測試數字，沒有真的跑一次完整流程）。所以我的建議是：**這輪先確認外頁的橫式/泰文/
百分比對不對，內頁如果還有落差就直接說哪裡**，如果來回幾輪還是卡在同樣的地方，
再認真討論要不要做拖曳編輯器這個獨立功能。
