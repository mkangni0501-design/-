# 交接文件 2026-08-10（延續前一輪：代課批次功能 + 聘書PDF/Word）

## 套用步驟（必做）

1. 到 Supabase 後台 SQL Editor，執行 `sql/36teacher_letter_settings.sql`（新增，存放校名/校長/董事長/電話/地址）。
2. **不再需要**之前交代的 `teacher-letter-templates` Storage bucket——這輪已經把「上傳Excel樣本檔」整個換成直接產生PDF/Word，不需要存Excel樣本檔了，如果已經建立這個bucket，留著不用也沒關係，之後也可以砍掉。
3. 部署新版程式碼。

---

## 1. 代課安排頁：補上節次/代課教師顯示 + 同一天批次排代

**根因**：原本近期代課紀錄那張表用 PostgREST 的外鍵關聯語法（`teachers!xxx_fkey(name)`）撈教師姓名，這種寫法在關聯快取沒更新時容易整欄查不到、顯示空白。

**修正**：改成分開查代課紀錄（只帶自己的欄位，節次本來就讀得到）跟教師名單，自己用 Map 對應姓名，不再依賴容易出問題的關聯語法。另外把「新增代課安排」整個改成「同一天批次代課安排」——先選日期，可以一次加入好幾位當天請假的老師，每位各自勾節次、各自指定代課教師，最後一次批次送出全部建立。

## 2. 聘書相關修正

### a/b/c. 三份文件改用「0808.xlsm」內容 + 輸出PDF/Word

已經逐條比對「0808.xlsm」的「印在職證明」「印自聘教師聘書」「印當年聘書」三張工作表的公式（VLOOKUP查資料、DATEDIF算年資、[DBNum1]轉中文數字、校長/董事長簽署邏輯等等），用一般 TypeScript重新實作成 `lib/teacherLetterContent.ts`，PDF用 `@react-pdf/renderer`、Word用 `docx` 套件產生，兩種格式共用同一份算好的內容，不再產生Excel檔。

原本Excel裡寫死的校名/校長/董事長/電話/地址，改成存進新的 `teacher_letter_settings` 資料表，「列印」分頁裡可以直接編輯、存檔，不用再改程式碼或上傳新樣本檔。

**中文PDF字型**：之前 `lib/ReportCardDocument.tsx` 就提醒過 react-pdf 沒有內建中文字型，會印出空白——這次一併補上了，字型檔（Noto Sans TC）放在 `public/fonts/`，PDF文件元件裡已經註冊好，聘書/在職證明的PDF現在可以正常顯示中文。

**幾個從Excel搬過來時做了簡化/修正的地方**（已盡量比照原邏輯，但有些原始公式太依賴Excel底層機制，改成更穩定的寫法）：
- 「發文日期」原本是用「聘期起算日+119天」這種公式湊出來的，改成用「發聘時間」欄位（自聘教師）或當天日期（當年教師，原表沒有獨立的發聘時間欄位）。
- 在職證明的任職起訖時間，原表最後一段沒填迄日時是顯示某個計算日期，其餘兩段沒填是顯示「至今」；這裡統一都顯示「至今」，比較好懂。

### d. 列印頁選取功能失敗

**根因抓到了**：下拉選單原本用「序號」當選取值，但序號可能是空的（NULL）或有人不小心填重複，好幾筆資料的選單值就會變成同一個，選了A老師實際上印出來卻是B老師的資料（或印出空的）。

**修正**：改用每一筆「歷年教師資料」保證唯一、不會是空值的內部ID來選取，序號只在按下「下載PDF/Word」那一刻才另外查出來填進文件內容，不會再對不起來。

### e. 已離職不出現在當年教師資料

已修正，「當年教師資料」現在會排除「歷年教師資料」裡「離職」已勾選的教師（「自聘教師資料」維持原樣不排除，因為那邊本來就有自己的「離職」欄位可以顯示）。

### f. 自聘聘期預設值

比對「0808.xlsm」的實際公式後，發現這個規則比「固定下一年」更精細：**如果現在已經過7月，起聘年抓明年；否則抓今年**。已改成照這個邏輯算，今天（2026/8）算出來是2027年，所以現在的預設值是2027/1/1～2027/12/31，結果跟原本一樣，但邏輯本身更準確（例如：如果現在是2026年3月才建資料，就會抓2026/1/1～2026/12/31，不是2027年）。

### g. 當年聘期預設值：「5/1～次年4/30」已確認正確

原本的自聘/當年兩份預設值的「起聘年」其實是同一個（當年的5/1，落在跟自聘同一個「參考年」裡，而不是單純看今天日曆上是哪一年），已經照這個邏輯一併修正——現在（2026/8）算出來，自聘預設2027/1/1～2027/12/31，當年預設2027/5/1～2028/4/30，兩者的起聘年是同一年（2027），對得起來。

---

## 本輪修改/新增的檔案

- `app/(app)/admin/substitute-teaching/page.tsx`（整篇重寫：穩健查詢 + 同一天批次代課）
- `lib/teacherLetters.ts`（e：當年教師資料排除離職；f/g：聘期預設值改用精確的參考年邏輯；移除舊的Excel樣本上傳功能）
- `lib/teacherLetterContent.ts`（新增，比照0808.xlsm公式算出列印內容，PDF/Word共用）
- `lib/TeacherLetterPdfDocuments.tsx`（新增，react-pdf文件排版，含中文字型註冊）
- `lib/TeacherLetterDocx.ts`（新增，docx套件產生Word文件）
- `lib/teacherLetterSettings.ts`（新增，校名/校長/董事長/電話/地址設定的讀寫）
- `components/dev-tools/TeacherLettersPanel.tsx`（PrintSection整個重寫：PDF/Word下載、d的選取bug修正）
- `public/fonts/NotoSansTC-Regular.woff2`、`NotoSansTC-Bold.woff2`（新增，中文PDF字型）
- `sql/36teacher_letter_settings.sql`（新增，**待您手動執行**）
- 移除 `public/templates/teacher_letters_print_template.xlsm`（不再需要Excel樣本）

已用 `npx tsc --noEmit` 檢查過，本輪修改的檔案沒有新增型別錯誤（專案裡另外還有2個跟這次改動完全無關、原本就存在的型別錯誤，在 `app/api/reports/report-card/` 底下）。
