# 修正說明（2026-08-08 第二輪）

依當天2項需求逐一處理，以下說明改了哪些檔案、為什麼、以及上線前要注意什麼。

---

## 1. 出缺勤批次上傳失敗

**用「成績出缺0808.xlsx」實際比對後找到的根本原因**：
這份檔案「成績、出缺輸入表」第5列的日期表頭（例如「5月10日」）是**純文字**，不是Excel日期格式
（用 openpyxl 檢查儲存格：`data_type='s'`、`number_format='General'`）。但
`lib/scoreAttendanceSheetParser.ts` 的 `findAttendanceDateColumns()` 原本只認得：
- 真正的 Excel 日期物件（`Date` instance）
- Excel 日期序號（數字 > 40000）

遇到純文字日期就整批讀不到，`dateColumns` 變成空陣列，於是迴圈直接跳過、什麼都沒寫入資料庫，
畫面卻顯示「成功匯入 0 筆」、沒有任何錯誤訊息——使用者看到的就是「上傳了但沒反應」，
以為是系統壞掉。

**修正內容**：
- `lib/scoreAttendanceSheetParser.ts`：`findAttendanceDateColumns()` 新增支援「5月10日」這種
  純文字日期格式，用該班級所在的「學年度」（呼叫端傳入）推算西元年份；日期是由左到右依時間
  先後排列，所以用「月份比前一欄小」代表跨過年底（12月→1月），可以正確處理橫跨兩個西元年份
  的學期，同時不影響像樣本檔案「5月10日～10月10日」這種全部落在同一西元年份內的情況。
- `app/(app)/attendance/weekly/page.tsx`：呼叫 `findAttendanceDateColumns` 時多傳入
  `header.academicYear`；並且在讀不到日期欄位／讀不到學生資料時，改成明確的錯誤訊息
  （而不是像過去那樣「成功匯入 0 筆、無錯誤」的無聲失敗），避免使用者誤以為系統壞掉。

**上線前請注意**：這個修正只處理「純文字日期」格式；如果貴校日後改用系統自己「下載出缺席輸入
範本」產生的檔案（Excel 真正的日期格式），一樣可以正常讀取，兩種格式都支援。

---

## 2. 開發人員頁新增「聘書分類」（批次上傳／下載／編輯）

以「0509教師資料_VBA列印.xlsm」為樣本，把原本要開Excel、按VBA巨集才能列印的教師資料，
改成在開發人員頁直接管理。原始活頁簿有6張工作表：

| 原始工作表 | 性質 | 這次的處理方式 |
|---|---|---|
| 在職證明 | 資料表 | 建新資料庫表 `teacher_service_certificates`，網頁管理 |
| 印在職證明 | VBA列印排版 | 不建表（不是資料本身），改用「下載目前資料」的Excel整份列印 |
| 自聘教師資料 | 資料表 | 建新資料庫表 `teacher_appointment_letters`（category='自聘教師聘書'） |
| 印自聘教師聘書 | VBA列印排版 | 同上，不建表 |
| 當年教師資料 | 資料表 | 併入 `teacher_appointment_letters`（category='當年教師聘書'） |
| 印當年聘書 | VBA列印排版 | 同上，不建表 |

「自聘教師聘書」跟「當年教師聘書」欄位高度重疊（姓名/職位/性別/聘期/起訖日期），比照系統
既有「總務處5張表」的做法，共用一張資料表、用 `category` 欄位區分，「自聘教師聘書」多出的
「離職」「發聘時間」兩欄兩張表都保留、「當年教師聘書」不用填即可。

**新增的檔案**：
- `sql/33teacher_letters.sql`：新增 `teacher_service_certificates`、`teacher_appointment_letters`
  兩張表，權限比照本頁其他表格（backups／account_audit_log），只開放系統管理員或歸屬
  「開發人員(dev)」部門的帳號可以讀寫（`is_system_admin() or has_department('dev')`）。
  **上線前記得先在 Supabase 執行這份 SQL**（照編號順序接在 32 後面）。
- `lib/teacherLetters.ts`：3個分類（在職證明／自聘教師聘書／當年教師聘書）的Excel範本產生、
  批次上傳解析、「下載目前資料(現況)」查詢、逐筆新增/編輯/刪除的函式。另外附上
  `computeServiceDuration()`：依原檔「在職證明」的算法（把最多3段任職/離職期間的年/月/日
  分別相加，沒有離職日期的那一段用「計算日期」當結束日）試算在職時間，只用在「下載目前資料」
  時附加顯示欄位，供列印/歸檔參考，不影響資料庫實際存的日期欄位本身（這個簡化加總不會做
  進位處理，比對過樣本檔案幾筆真實資料計算結果一致，但仍建議正式列印前人工覆核一次）。
- `components/dev-tools/TeacherLettersPanel.tsx`：開發人員頁的「聘書分類」區塊，3個分類分頁，
  每個分頁都有「下載範本」「下載目前資料(現況)」「批次上傳」，加上表格列表可以逐筆
  「編輯」「刪除」，以及「＋新增一筆」。
- `app/(app)/admin/dev-tools/page.tsx`：掛載上面的元件，放在「一鍵上傳／下載」區塊下方。

**測試方式**：已用專案本身的 `tsconfig.json` 跑過 `npx tsc --noEmit` 及 `npx next build`，
新增/修改的檔案都沒有型別或建置錯誤（剩下2個錯誤是 `app/api/reports/report-card` 底下既有、
跟這次修改完全無關的舊問題，上一輪交接文件已經記錄過）。資料庫層面的行為（新表的
批次上傳/編輯/刪除）建議部署、跑完 `sql/33teacher_letters.sql` 後，用測試帳號（開發人員或
系統管理員S身分）實際跑一次三個分類的「下載範本→填資料→上傳→查看列表→編輯→刪除」
再正式上線。
