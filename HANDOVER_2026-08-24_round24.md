# 2026-08-24 追加修正（第二十四輪）：學生登入代碼格式、帳號身分欄位污染、
# 家長查詢效能、社團模組複查、成績單三處版面

這輪的成績單三處，全部用 `renderToFile` 實際渲染 + `pdftoppm` 轉圖 + 量測像素
的方式驗證過（跟 round23 同一套方法），不是憑肉眼掃過去猜的；也因此意外多挖出
兩個原本沒被抓到、跟這次要修的東西同一批程式碼裡的既有 bug，一併說明在下面。

## 一、學生登入代碼改成 HYS+學號（原本是 HY+學號+S）

`app/(app)/admin/students/portal-accounts/page.tsx` 的代碼產生邏輯，字尾 `S`
改成字首 `HYS`：家長維持「HY+學號」，學生本人從「HY+學號+S」改成「HYS+學號」。

**已經建立過的舊格式帳號怎麼辦**：`sql/55fix_portal_account_relation_and_student_code_format.sql`
會把所有 `relation='學生本人'` 的既有帳號，login_code 直接依 student_no 重新產生成
新格式。⚠️ **這會讓已經發出去的舊代碼（HY...S 字尾）失效**——`app/portal/login/page.tsx`
每次登入都要重新輸入代碼跟信箱比對，不是只有第一次綁定才需要，所以這批學生
上線後需要重新通知新代碼；已經完成綁定（auth_user_id 已寫入）的帳號不用重新
綁定，只是代碼本身要換新的告訴學生。家長帳號完全不受影響。

## 二、學生登入後找不到對應資料——根因：帳號建立頁的「帶入信箱」按鈕會污染身分欄位

用同一份 `portal-accounts/page.tsx` 追查，發現「帶入{監護人}信箱」這顆方便按鈕
除了帶入 email，還會用 `setRelation(g.relation)` 把「身分」下拉選單（合法值只有
「家長」／「學生本人」兩種）直接覆蓋成 `guardians` 表裡的監護人關係（父／母／
監護人…），這兩個欄位語意完全不同，混用會把不對的值存進
`portal_accounts.relation`。

**這怎麼造成「學生登入後找不到資料」**：任何依賴 `relation = '學生本人'` 判斷
「這是學生自己的帳號」的地方（例如這次新增的社團選社頁，只讓學生本人身分的
登入者填志願序）都會找不到這筆帳號，登入本身是成功的（email+代碼比對不受
影響），但功能/資料對不上，使用者看起來就像「帳號登入了卻沒東西」。

**修正**：
- 按鈕現在只帶入 email，不再動 relation（見 `page.tsx` 內註解）。
- `handleCreate` 送出前多一層防呆：relation 不是「家長」或「學生本人」就直接
  擋下不送出。
- 資料庫加上 `check (relation in ('家長','學生本人'))` 限制，以後不管哪個頁面
  或腳本誤寫都會被資料庫層擋掉，不用等到某個依賴 relation 篩選的功能「看起來
  找不到資料」才發現。
- `sql/55...sql` 會先把既有已經污染的資料修正回 `'家長'`
  （這個 bug 底下 login_code 其實沒受影響，還是對的「HY+學號」格式，只有
  relation 這個標籤本身錯了，所以只需要修 relation，不用連 login_code 一起改）。

## 三、家長查詢功能速度緩慢——根因：排名 view 每次都先算過全校才篩選

`app/(app)/portal/page.tsx` 讀「歷年成績＋班排名＋年級排名」原本是對
`sql/6portal.sql` 的 `class_rankings`／`grade_rankings` 這兩個「全校通用」view
下 `.in('enrollment_id', [...])`。這兩個 view 內部的 `rank() over (...)` 是先把
「全校」所有班級/學生的排名整個算完，篩選是在最外層、算完之後才發生——不管一個
小孩實際只讀過幾個學期，資料庫每次都得先處理全校規模的資料。
`sql/44fix_report_card_and_ranking_performance.sql` 那輪的優化解決的是「同一批
`exam_type_locked()` 判斷被重複呼叫」，沒有解決這個更根本的「範圍從頭到尾都是
全校」的問題，所以家長查詢頁還是慢。

`sql/50fix_ranking_query_timeout.sql` 那輪其實已經示範過同樣手法的解法
（`class_rankings_for_class`／`grade_rankings_for_class`，把範圍先縮小到「一個
班」再算），只是那輪只處理教師視角（查一整班/一整個年級），沒處理家長/學生
查詢入口這種「查一個學生的歷年紀錄」視角。

**修正**：`sql/56portal_scoped_academic_history.sql` 新增
`portal_student_academic_history(p_student_no text)`，一次把「這個學生讀過的
每個學期」各自所屬的「班級」「同部別年級」範圍先抓出來（通常只有幾十~一兩百
人，不是全校1300多人）再算排名，一次 RPC 呼叫就把總分/班排名/年級排名全部
算好回傳。`portal/page.tsx` 原本的 3 個查詢（`student_total_scores` /
`class_rankings` / `grade_rankings`，各自 `.in(...)` 再用 Map 兜資料）改成呼叫
這支新函式。權限規則跟 `class_rankings_for_class`／`grade_rankings_for_class`
一致（管理員/教務全開放、導師限自己班、家長/學生限自己綁定的學號）。

## 四、社團功能複查

`sql/54clubs_module.sql` 還沒實際安裝過，這輪重新逐項檢查邏輯，抓到兩個問題，
已經直接改在同一個檔案裡（不用另外疊修正）：

1. **`app/(app)/clubs/grading/page.tsx` 的「暫存」會悄悄解鎖已送出的學生**：
   原本不管按「暫存」還是「正式送出」，都把畫面上所有學生（包含已經正式送出
   鎖定的）一起 upsert，`is_submitted` 統一設成這次按鈕的值——點「暫存」時會
   把已經送出鎖定的學生的 `is_submitted` 從 `true` 寫回 `false`，等於悄悄解鎖；
   即使 RLS 本來就只允許在 `is_submitted` 還是 `false` 時更新，一般社團老師
   對已鎖定列的更新会被 RLS 擋下，可能讓整個 upsert 失敗或部分失敗。改成
   `saveAll` 一開始就把已經送出鎖定的學生排除在外（`editableRows`），不管
   暫存或正式送出都只處理還沒送出的部分；全部都已送出時，兩個按鈕都停用並
   顯示提示訊息。
2. **隨機亂數抽籤法（`run_club_lottery_random_number`）沒有檢查社團是否還
   `is_active`**：第一志願優先法（`run_club_lottery_priority`）的迴圈本來就有
   `and is_active` 條件，隨機亂數法漏掉了，如果學生填志願序之後某個社團被
   教務處停用，隨機亂數法仍可能把學生分發進去。已加上一致的檢查。

其餘（志願序分發、即時搶選的 advisory lock 防超收、`club_member_counts()`
不外洩個別學生資料、点名/成績表跟排課系統分開、`enrollments.term` 對應現在
學年學期的查法等）複查過邏輯正確，沒有再發現問題。

## 五、成績單三處修改

### a. 【家長簽章及建議】左邊【升留級】沒印出來

根因：原本這格標籤是 `spring && fall ? labels.promotionStatus : ''`，只有印
「下學期／學年」成績單（上下學期都有資料）時才顯示「升留級」三個字，這次的
範例（高三忠、只印上學期）剛好落在條件外，整格連標籤都是空的。升留級本身
目前確實沒有資料來源可以填值（維持留空），但標籤不該跟著隱藏——已改成標籤
永遠顯示。

### b. 導師/訓導/教務/校長簽章對齊紀律

這個是這輪意外挖到的更深問題：round23 宣稱已經修好的「簽章列跟紀律對齊」，
其實是巧合──`AttendanceDisciplinePanel`（右邊）的簽章列用 `flex:1` 撐滿剩餘
高度沒錯，但左邊 `ScoreTable`（科目成績表）本身是一個普通、不會跟著縮放的
`<View>`，高度完全看科目數量的自然內容高度決定。round23 測試時剛好用的科目
數量，讓左邊表格自然高度跟右邊撐滿的高度差不多，兩者才會對上；換一個科目數
不同的班級（例如這次的高三忠），左邊表格的自然高度就會跟右邊撐滿的高度對不
上，紀律的底線跟簽章列的底線就會分開。用 15 科的測試資料實際渲染，還發現
更嚴重的：科目一多，整張表格自然高度直接超過一頁，react-pdf 會自動多開一頁
把內頁印成兩頁，右邊出席/懲獎面板卻還留在第一頁——不只對不齊，版面直接錯開。

修法：把「科目列＋學業平均＋操行成績」這個數量會變動的區塊，改成用 `flex`
依比例分配剩餘高度（操行成績5列給 `flex:5`，其他每列 `flex:1`），外層包一個
`flex:1`，不管科目有幾科，這個區塊永遠精準撐滿可用高度的100%——「紀律」的
底線因此永遠等於右邊簽章列撐滿的那個底線，不會再因為科目數不同就跑掉。

實際測試發現 react-pdf 底層的 Yoga 排版引擎有個限制：就算列設定
`minHeight:0` + `overflow:'hidden'`，還是不會把列高縮到比文字自然高度更矮
（這是已知限制，不是這裡 flex 設定寫錯）。科目數超過12科左右時，光用 flex
沒辦法真的把每列壓進可用空間，還是會溢出到第二頁。所以另外加了
`denseScale`：科目數超過9科時，字體跟内距一起等比例縮小（下限0.5倍），科目
不多的班級完全不受影響，維持原本字級；已經用 5、12、15、20 科分別實際渲染
+ 量測像素驗證過，各種科目數都精準落在同一條底線、都是精準一頁。

### c. 外頁文字放大且置中對齊

外頁（正面三折頁）原本大部分文字靠左（右折頁的校名區塊是靠右對齊），字級也
偏小。這次統一：三個折頁的所有文字都置中，字級整體調大（依原本層級關係微調，
標題最大、內文次之）。

過程中發現並修正兩個連鎖出現的既有排版 bug（不是這次新增的問題，是這次把
字級調大之後才會踩到）：
1. 幫某個區塊加上 `alignItems:'center'` 之後，裡面「特殊表現計算方式」的
   嘉獎/警告雙欄清單整個壞掉（兩欄文字疊在一起、還逐字換行）——追出原因是
   `flexDirection:'row'` 的容器本身沒有明確寬度，父層一旦是
   `alignItems:'center'`（縮到內容寬度）而不是預設的 `stretch`，兩個
   `flex:1` 子欄位就會因為抓不到父層的明確寬度陷入 Yoga 的循環依賴計算，
   寬度被算成接近0。修法是給這個 row 明確加上 `width:'100%'`，不依賴父層
   的 stretch 行為。
2. 加大字級後，中欄底部（學業成績計算方式＋特殊表現計算方式）內容超出
   36%高度的區塊，用同一套渲染+量測流程重新校準了字級跟行距/間距，目前
   在 12 科、上下學期都有資料的測試案例下驗證是精準兩頁（外頁+內頁），
   沒有多跳出空白頁。

## 上線前要做的事

1. 依序執行順序不變，`sql/54clubs_module.sql`（這次已經是修正後的完整版，
   照樣先跑）→ `sql/55fix_portal_account_relation_and_student_code_format.sql`
   → `sql/56portal_scoped_academic_history.sql`。
2. **`sql/55...sql` 執行後，已經領過舊格式代碼（HY...S字尾）的學生要重新
   通知新代碼（HYS+學號）**，否則舊代碼會登入失敗。已經完成綁定的帳號不用
   重新綁定。
3. 確認過去是否有承辦人員用「帶入{監護人}信箱」按鈕建立過「學生本人」帳號
   （relation 欄位這次會被自動修正回「家長」）——如果真的有學校要建學生本人
   帳號時誤按了這顆按鈕，這批帳號這次會被誤判、修正成「家長」，建議上線後
   跟教務處核對一次已建立帳號清單，確認每一筆的「身分」都跟學校記錄相符，
   有需要的話手動改成正確的身分並重新產生代碼。
