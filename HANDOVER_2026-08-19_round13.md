# 2026-08-19 追加修正（第十三輪）：批次列印/個人列印按了沒反應（真正抓到了）、聘書字型錯誤根因確認並修正

這輪沒有新增 sql 檔案，全部是前端／API 程式修正，安裝上跟平常一樣整包部署即可。

## 1、2. 批次列印／個人列印按了沒反應

**這次真的抓到了，不是猜的**：問題出在 `window.open(...)` 這一行寫的位置。程式原本
是「先 `await` 打 API、等 PDF 資料回來，才呼叫 `window.open()` 開新分頁」——但瀏覽器
的「彈出視窗封鎖」機制，只認「使用者點擊的當下、還沒經過任何等待」才算是「使用者
主動要開新分頁」，只要中間經過 `await`（哪怕只有零點幾秒），瀏覽器就會把之後的
`window.open()` 當成「網頁自己偷開視窗」直接**靜靜擋掉，不會跳出任何錯誤訊息或
提示**——這就是「按了沒反應」的真正原因，而且這個問題同時存在於**三個地方**：
【班級成績總表】頁的批次列印按鈕、【班級成績總表】頁的個人列印按鈕、還有一個獨立的
【批次列印全班成績單】頁面（`components/admin-tabs/BatchReportCardTab.tsx`，也一併
修好了）。

修法：改成「使用者一按下按鈕，馬上（還沒有任何 await）先同步開一個空白分頁」，
等PDF真的產生好了，再把這個已經開好的分頁導向到PDF內容——這樣瀏覽器全程都認得
「這是使用者剛剛點的」，不會被擋。同時也補上了原本沒有的錯誤處理：如果 API 真的
失敗，現在會清楚顯示狀態碼跟錯誤訊息，不會再是「靜悄悄地什麼都不顯示」。

**如果瀏覽器本身設定成「一律封鎖彈出視窗」（不是因為 await 的問題，是使用者自己
設定的），現在會跳出明確的提示文字**告訴你要去瀏覽器設定允許彈出視窗，不會再是
完全沒反應。

## 2（續）. 管理者S還是看不到總分排名

這輪沒有再改 SQL（你確認過 sql/47/48/49 都跑過、role 也對，我已經黔驢技窮，
再猜測性地重寫第三次類似的 SQL 意義不大）。改成**讓程式自己在出錯的時候說出真正
的錯誤訊息**：【班級成績總表】頁如果查詢總分/排名時失敗，現在畫面上會直接顯示
紅色的錯誤提示框，把 Supabase 回傳的實際錯誤內容印出來——**麻煩用管理員S帳號
重新整理一次頁面、切到「高三忠」，把畫面上出現的錯誤訊息截圖或文字給我**。原本
是「查詢失敗也不會有任何提示，只會看到空白」，這次不管是不是同一個問題，至少會
真正知道卡在哪裡，不用再靠我這邊憑空重寫SQL用猜的排查。

## 3. 聘書「Unknown font format」＋沒有直接列印功能

**這次找到真正的根因了**：在職證明／聘書的 PDF，原本是**直接在瀏覽器裡**產生的
（`components/dev-tools/TeacherLettersPanel.tsx` 呼叫 `pdf(...).toBlob()`），不是
像成績單那樣在伺服器端產生。中文字型註冊（`lib/pdfFonts.ts`）用的是 Node.js 才有
的 `path.join(process.cwd(), ...)` 去讀伺服器上的字型檔案——這段程式碼在瀏覽器裡
執行時，`process.cwd()`／檔案路徑完全對不上，react-pdf 抓到的東西不是正常的字型
二進位內容，才會出現「Unknown font format」。成績單的PDF因為本來就是在伺服器端
（API Route）產生的，同一套字型註冊反而正常運作，這次直接把兩者的行為差異抓出來，
也用同樣的方式（把測試資料餵進程式、實際請電腦產生PDF）驗證過。

修法：新增了一個伺服器端的 API（`app/api/reports/teacher-letter/route.tsx`），把
PDF「產生」這一步從瀏覽器搬到伺服器端，跟成績單走同一套已經驗證沒問題的架構。
順便一起解決「沒有直接列印功能」——原本不管有沒有字型問題，都是強制觸發下載一個
檔案，不會在瀏覽器分頁裡開啟可以直接看、直接按 Ctrl+P 列印的PDF；現在跟成績單
一樣，按下「產生PDF」會開一個新分頁直接顯示PDF內容，可以直接列印或另存。
Word（docx）格式維持原本下載的方式不變（本來就是「下載檔案」而不是「預覽列印」
的性質，不需要改）。

**這個修正需要你實際測試確認**——我這邊用測試資料驗證過「伺服器端產生PDF」這個
機制本身沒問題（跟成績單共用同一套字型檔案／同一種產生方式），但沒辦法連到你們
的正式環境實際按一次「產生在職證明PDF」，麻煩部署後找一位老師的資料實際試一次。

批次列印聘書（一次列印多份）這次還沒有做，上一輪就提過這是全新功能，這輪先把
「單份聘書PDF根本不能用」這個更急迫的問題處理掉，批次列印留到之後。

## 版面轉換

你提到「先不要管封面文字，做成能讓我修正文字的版本就好」——收到，了解意思是：
不用糾結封面說明文字（操行基本分80分那段）跟實際 Excel 公式的差異要不要一致，
先做出一個「文字內容你自己可以改」的版本即可，不用我先幫你把兩邊邏輯對齊。這點
會留到下一輪動版面的時候一併處理（這輪全部時間都花在抓「按了沒反應」「字型錯誤」
這兩個根因上，沒有餘裕再展開版面工作，但這兩個問題解決後，下一輪應該可以比較
專心處理版面）。
