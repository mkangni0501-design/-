# 2026-08-17 追加修正（第九輪）：出缺席分數公式重寫、個人成績單按鈕收斂、評語輸入、Excel下載除錯

延續第八輪（`HANDOVER_2026-08-16_round8.md`）；這輪的四個反映事項對應你附的兩張截圖
（白色日期＝目前系統產出的成績單，綠色日期＝貴校目前實際使用、要對齊的樣本）。

安裝順序：`sql/48fix_attendance_score_formula.sql` 需在 `sql/47...sql` 之後執行。

## 1＋2. 出缺席分數公式整個重寫（原本的設計方向錯了，不是小修）

**根因**：上一輪（sql/46, sql/47）把「出缺席」當成算完總分之後「額外加減一個固定值」
來處理（`attendance_adjustment()`：全勤→比重×100，例如+3分；有缺勤→直接扣原始扣分
點數，例如-1.3分——兩種情況公式還不一致），而且這個調整值只加在「總分」這一個數字
上，完全沒有反映到期中/期末/平時個別欄位。這正是這輪你點出的兩個問題的根本原因：
「扣分應該從0開始，100只有全勤才有」＋「學業平均期中/期末/平時沒顯示、總分公式錯」。

**改成**：出缺席現在是一個「正常的科目」，用 `attendance_score()` 算出分數（全勤=100，
否則＝【整體佔比與加扣分規則】扣分點數直接加總，不設下限），期中/期末/平時三欄放
同一個值（出缺勤紀錄本來就是整學期累計、不分期中考/期末考時段），然後跟其他科目
一樣正常乘上科目比重（3%）、依期中35%/期末35%/平時30%合併——用的是系統原本就有的
`subject_weighted_scores`／`student_examtype_totals` 這一整套邏輯，不再是另外寫一套
「調整值」規則。老師在成績登錄頁如果還是手動輸入了「全勤」這個科目的分數，那個值
不會被使用（原本就有的提示文字還在）。

`class_attendance_adjustment_batch()`（【班級成績總表】顯示用）、`lib/reportCard.ts`
的成績單「出缺席」欄位，都改用新的 `attendance_score()`，不再用「100+調整值、最低0」
這種算法。

**請幫忙驗證**：這輪沒有你資料庫的真實數字可以核對，只能照你描述的公式邏輯實作。
麻煩套用後找一兩個像蔡智強這樣有出缺勤紀錄的學生，核對「學業平均」期中/期末/平時
三欄的數字，跟你手上照 Excel 手算的結果是否一致；如果對不上，麻煩把手算過程的
中間數字（例如：各科加權小計是多少、出缺席算出來是多少、乘上3%後是多少）貼給我，
比較好抓是哪一步的公式還是不對。

## 2（後半）. 導師評語輸入的地方

原本整個系統都沒有任何畫面可以輸入導師評語（`student_remarks` 表只有讀取／匯入時
清空的程式碼，沒有輸入介面）——不是「藏在別的頁面」，是真的從來沒有做輸入框。
已經照你的要求加在【成績登錄】頁：選到「平時分」時，會出現一個可以收合的
「📝 輸入導師評語」區塊，每位學生一個文字框，離開輸入框自動存檔；只有導師本人或
管理員看得到（跟成績單的權限一致，任課教師看不到）。

## 3. 期中/期末/平時三個分頁不要有「列印個人成績單」按鈕＋Excel下載失敗

- 【班級成績總表】頁：期中考/期末考/平時分這三個分頁，個人「列印」按鈕已經拿掉
  （改顯示「—」），只有切到「全部」分頁才看得到列印按鈕；期中/期末/平時這三個
  分頁的個人資料，改用「下載成績 Excel」取得。
- 「downloadClassScoreExcel is not a function」這個錯誤，檢查程式碼本身沒有問題
  （函式確實有正確 export、import 路徑也對）——這種 `WEBPACK_IMPORTED_MODULE... is
  not a function` 的錯誤，最常見的原因是**執行環境還在用舊的建置結果**（`lib/excelTemplates.ts`
  這個檔案更新了，但 `.next` 的建置快取、或是瀏覽器快取的舊 JS 檔案沒有跟著換掉）。
  麻煩部署這次的新程式碼時，確認有清掉 `.next` 快取重新建置（`rm -rf .next && npm run build`
  這類流程，實際指令依你們的部署方式而定），瀏覽器端也建議硬重新整理（Ctrl+Shift+R）
  一次再測試。如果重新建置後還是同樣的錯誤，麻煩告訴我，我再進一步排查。

## 4. 兩張截圖的說明

已理解：白色日期那張是目前系統（有問題的地方，例如出缺席公式、學業平均空白）；
綠色日期那張是貴校目前實際在用、系統應該要對齊的樣本格式與計算邏輯。這輪主要是
照綠色日期那張描述的邏輯去修正計算公式，版面本身（欄位位置/大小）還沒有調整——
如果套用這輪修正、數字都對上之後，版面還有哪裡跟綠色那張不一樣，麻煩再告訴我
具體是哪幾個欄位。

## 這輪還沒處理的部分

- 上一輪提到的「拖曳版面編輯器」（反映事項3）這輪還是沒有做，等你確認方向。
- `attendance_adjustment()` 這支舊函式先保留沒有刪除（避免有地方還在呼叫時噴錯），
  確認没有任何地方在用之後可以清掉。
- 操行成績（禮貌/衣著/服務/紀律＋懲獎加減分）目前你截圖裡是空白，這輪沒有觸碰這
  部分的計算，如果也需要對齊綠色樣本的邏輯，麻煩下次一併告訴我。
