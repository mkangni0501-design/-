# 2026-08-18 追加修正（第十二輪）：實際跑過PDF產生測試、獎懲確認、管理員S權限持續追查

這輪沒有新增/修改任何 sql 或 .tsx 檔案（找不到需要改的地方），主要是**用你上傳的
AI.xlsx 真實數字，實際跑了一次 PDF 產生流程**（不是只用眼睛看程式碼），確認了幾件
重要的事，說明如下。

## 1. 管理者S還是看不到總分排名

上一輪（sql/49）已經把判斷邏輯改成跟系統其他地方統一，但你這次確認還是不行。
我沒辦法連到你的資料庫，只能請你幫忙確認兩件事，才能繼續往下抓：

**(a) 先確認 sql/47、48、49 三個檔案都已經依序執行成功。** 這個專案目前已經有到
`sql/49fix_admin_s_ranking_visibility.sql`（見上面），麻煩在 Supabase SQL Editor 貼
下面這段確認這三個檔案新增的函式都存在：
```sql
select proname from pg_proc
where proname in ('report_card_any_locked', 'attendance_score', 'is_system_admin');
```
應該要回傳三列都有，缺任何一個都代表對應那個檔案沒有跑過（或跑失敗），需要先補上
再繼續（前幾輪已經遇過兩次類似的漏執行問題，這次先請你確認排除這個可能）。

**(b) 如果三個函式都確認存在、還是看不到，麻煩查一下系統管理員S這個帳號在資料庫
裡實際存的角色值：**
```sql
select id, role, name from app_users where role = 'system_admin_s';
```
如果這個查詢完全沒有結果，代表你在用的那個「管理員S」帳號，它的 role 欄位存的值
跟系統認定的 'system_admin_s' 不一樣（可能是帳號建立時選錯、或是舊資料），這是
資料本身要修正，不是程式或政策要再改，麻煩把查詢結果告訴我，我再確認下一步。

## 2. 批次列印/個人列印無法使用

**這裡有個好消息，值得說明一下我怎麼確認的**：我把 `ReportCardDocument.tsx`
這個「負責把資料排版成PDF」的程式，單獨抓出來，直接餵給它一組跟你 AI.xlsx
數字完全一樣的測試資料（蔡智強、出缺席-42.82、學業平均71.28...），**實際請電腦
產生了一份PDF**，再把PDF裡的文字抓出來核對——結果完全正確，中文正常顯示、
數字跟你的 AI.xlsx 一模一樣，操行等第「丙」也正確顯示。這代表「產生PDF」這一步
的程式本身沒有壞掉。

既然產生PDF這一步是好的，「批次列印/個人列印無法使用」比較可能是**卡在產生PDF
之前的某一步**，最可能是下面兩種情況之一：
- 跟第1點一樣，是同一個「管理員S的權限判斷」問題（`canAccessClass()` 這個函式
  也是直接比對 `role === 'system_admin_s'`，如果第1點的資料問題屬實，這裡也會
  一起失敗）；
- 前面sql檔案（尤其sql/47/48新增的 `report_card_any_locked`／`attendance_score`
  這幾支函式）還沒有在你的資料庫執行成功，導致查資料那一步就先出錯了。

麻煩先確認第1點的兩個檢查，如果那邊都正常、這裡還是不行，麻煩告訴我**實際按下
「列印」按鈕之後看到的訊息**（是跳出「沒有權限」、還是「尚未能產出正式成績單」、
還是完全沒反應、或是瀏覽器主控台有錯誤訊息），這樣我才能確定是卡在哪一步，而不是
繼續憑空猜。

「應該要能用在【全部】頁」——這點已經是現在的行為（上一輪拿掉的只是期中/期末/平時
這三個分頁「個人」列印按鈕，批次列印全班的按鈕本來就不分頁籤、任何時候都看得到，
不用特別切到「全部」才有）。

## 3. 聘書PDF「Unknown font format」

同樣**用你上傳的資料格式實際測試過**：把在職證明／聘書的 PDF 產生程式單獨抓出來，
餵測試資料實際產生PDF，結果**兩份都成功產生、中文正常顯示，沒有任何字型錯誤**。

這代表程式本身的字型設定是對的，问题很可能出在**部署到你們實際伺服器上的字型檔案
本身有問題**——「Unknown font format」這個錯誤訊息的意思是「程式想讀字型檔案，
但讀到的東西不是一個正常的字型格式」，最常見的原因是：字型檔案在部署過程中沒有
被正確複製過去（例如變成 0KB 的空檔案，或是被部署平台用某種壓縮/轉檔搞壞了，
或是用 Git LFS 一類的機制管理大檔案時，實際部署上去的是「指標檔」而不是真正
的字型檔案本身——這兩個字型檔案各自約 7MB，是比較容易踩到這類問題的檔案大小）。

麻煩幫我確認：你們實際運作中的伺服器上，`public/fonts/NotoSansTC-Regular.ttf` 跟
`public/fonts/NotoSansTC-Bold.ttf` 這兩個檔案是否**確實存在，而且檔案大小接近
7MB**（不是幾KB的小檔案）。如果檔案大小不對，麻煩重新把這兩個檔案部署上去；
如果檔案大小是對的、還是報一樣的錯，麻煩告訴我，我再進一步排查。

**批次列印功能**：確認 `components/admin-tabs/TeacherLettersPanel.tsx`／聘書相關頁面
目前**還沒有批次列印**功能（只能一次印一份），這是原本就沒有做的功能，不是壞掉，
這輪還沒有實作，會排在下一輪。

## 4. 獎懲加扣分在操行還是學業成績上——已確認，不用改

查證結果：系統目前的 `discipline_adjustment()`（嘉獎/警告/小功/小過/大功/大過的
加扣分）**只有一個地方會呼叫它，就是成績單的「操行成績」欄位**（`lib/reportCard.ts`
的 `conduct.overall`），沒有任何地方把它加進「學業成績」的總分/排名計算——這剛好
就是你這次希望的方向（放入操行分數中），不需要再改。

## 5. 版面轉換

這輪把時間都花在上面 1-4 項的實際驗證上（尤其是「跑一次真的產生PDF」這件事，
之前都只能用眼睛看程式碼，這次算是把工具建起來了，之後要驗證版面相關的修改
會快很多），版面本身（把 AI.xlsx／0808.xlsm 轉成正式的 HTML/PDF 版型）這輪沒有
進展，留到下一輪——上一輪發現的「操行成績公式」矛盾（封面文字說有懲獎加減、
實際 Excel 公式沒有）也還沒有你的回覆，麻煩這次一併確認一下，這樣下一輪動版面
的時候可以順便把這個也一起處理掉。
