# 2026-08-22 追加修正（第二十一輪）：用你附的樣本.xlsx逐格對照，抓到欄寬總和超過100%的根本問題

這輪沒有 sql 檔案，只改了 `lib/ReportCardDocument.tsx`。這次直接把你附的
`樣本.xlsx`「成績內」分頁的合併儲存格範圍跟數值都讀出來核對（不是憑螢幕截圖猜
位置），逐欄逐格比對過，跟前幾輪比起來這次應該準確很多。

## a. 格線沒有對齊——抓到根本原因

內頁表格「科目／比重／8個分數欄（期中期末平時總分×上下學期）／學年成績」這一整列
的欄寬**加起來超過100%**：科目欄14%＋比重欄7%＋8個分數欄（每個寫死9.5%＝76%）＋
學年成績欄10.5%＝**107.5%**，多出來的7.5%就是造成格線對不齊的根本原因（一整列
比容器寬，其他列自然對不上）。

已經改成：科目/比重/學年成績這三欄的寬度可以調整（上一輪加的功能），**8個分數欄
改成自動平分「扣掉那三欄之後剩下的空間」**，不管那三欄怎麼調整，整列永遠剛好等於
100%，格線就會對齊。同時把預設的欄寬比例也改成跟你的樣本檔案一致（樣本裡科目欄/
比重欄/學年成績欄其實是照 Excel 欄位數的比例：1:1:8個各1:2，換算成百分比＝
8.33%/8.33%/8.33%×8/16.67%）。

## b、c、d. 操行成績區塊重新對照樣本精確重排

樣本檔案的合併儲存格範圍告訴我精確的答案，不用再猜：

- 操行成績/禮貌/衣著/服務/紀律這五列的標籤，左邊**沒有**多留一截比重欄的空白——
  標籤直接佔滿「科目＋比重」兩欄合起來的寬度。上一輪的版本裡，這五列前面多了一個
  空白的比重欄（因為當初的排版邏輯跟其他列不一致），已經拿掉。
- 優/乙這些等第大字，正確位置是「**各自學期分數的右邊**」（上學期分數右邊接優，
  下學期分數右邊接乙），不是像上一輪那樣兩個等第擠在最右邊、緊接在下學期分數
  後面。換算成欄位比例：原本上學期整段（4個分數欄寬度）現在改成「2個分數欄給
  上學期數字、2個分數欄給優」，下學期比照辦理——等第的顯示空間也因此從固定17%
  變成跟著分數欄寬度走（欄寬變動時，等第的空間也會跟著變動，不會脫節）。
- 全學年平均維持在最右邊不變，五列都補上了正確對齊的數字。

這次用測試資料實際跑了一次 PDF，把文字抓出來核對，結果完全對上樣本檔案的欄位
順序（操行成績/禮貌/衣著/服務/紀律 → 上學期數字 → 優 → 下學期數字 → 乙 → 全學年
平均）。

## e. 家長簽章及建議旁邊是升留級

這部分程式碼原本就是對的（升留級在左、家長簽章及建議在右，25%/75%），不用改動，
這裡跟你確認一下對齊結果沒錯。

## 這次的驗證方式

這次能一次對齊，關鍵是你附的 xlsx 檔案保留了合併儲存格的實際範圍（例如「E18:F22」
代表「優」這個字精確跨了哪幾欄哪幾列），比純粹看截圖猜「大概在哪個位置」準確
非常多——之後如果還有版面要調整，附上這種「有實際儲存格結構」的樣本檔案（不用是
最終定稿，草稿也可以），會比截圖更快讓我一次對準，這點提供給你參考。
