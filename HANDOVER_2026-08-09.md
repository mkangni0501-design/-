# 交接文件 2026-08-09（本輪：管理員S反映的6項問題）

本輪處理管理員S回報的6個問題。以下逐項說明根因、修正內容、以及**套用這次更新後管理員S要做的事**。

## 套用步驟（必做）

1. 到 Supabase 後台 SQL Editor，執行新增的 `sql/35admin_role_department_backfill.sql`（其餘 SQL 檔案都已在正式環境執行過，只有這個是新的）。
2. 到 Supabase 後台 Storage，建立一個新的 private bucket，名稱為 `teacher-letter-templates`（比照 README.md 裡 `student-documents` bucket 的建立方式），用來存放「聘書列印樣本」上傳後的檔案。
3. 部署新版程式碼（`public/templates/teacher_letters_print_template.xlsm` 這個檔案已經換成使用者提供的「0509教師資料_VBA列印.xlsm」，記得這個檔案本身也要一起部署）。

---

## 1. 管理員S一鍵上傳「班級「高一」在對照表中找不到所屬年級」

**根因**：這不是程式邏輯的問題——`lib/gradeMapping.ts`、`lib/schedulerBridge.ts`、`public/scheduler/scheduler-tool.html` 三處的年級對照表本來就已經統一成正確格式（`高一/高二/高三`、`初一/初二/初三`，不帶「中」字；班級名稱是`高一忠`不是`高一`），跟您提供的 `校務0809.xlsx` 完全一致，實際測試也沒有重現這個錯誤。

問題出在專案資料夾裡混進了幾個**過時的測試用範例檔案**（`上傳檔案範本/` 資料夾下的4個檔案），這些檔案不是系統程式的一部分（程式碼完全沒有引用到它們），但其中「系統管理者一鍵匯入.xlsx」「校務系統完整資料快照_20260806.xlsx」等檔案還停留在改版前的舊格式（班級寫成「高一」少了「忠」、部別寫成「初中一/高中一」多了「中」字）。如果拿這幾份舊檔案去測試上傳，就會重現一模一樣的錯誤訊息。

**修正**：已將這個過時的 `上傳檔案範本/` 資料夾整個移除，避免之後又被誤用來測試。之後要驗證上傳功能，請用「一鍵下載」重新產生的當下快照檔案來測試，才會是最新格式。

## 2. 管理員S一鍵下載「帳號名單讀取失敗：column app_users.email does not exist」

**根因**：`app_users` 資料表本來就沒有 `email` 欄位——信箱是存在 Supabase Auth（`auth.users`），不是校務系統自己的資料表。`lib/currentDataSheets.ts` 的 `fetchCurrentAccountsSheet()` 卻直接 `select('name, email, role')`，從一開始就注定失敗。

**修正**：改成先查 `app_users` 拿到 id/姓名/角色，再呼叫系統本來就有、「帳號管理」頁面也在用的安全API（`/api/admin/list-accounts-with-email`）把信箱補回來。查信箱失敗也不會讓整張表「讀取失敗」，姓名/角色照常列出，只是信箱欄位留空。

## 3. 代課功能無法進行，應結合空堂教師查詢

**根因**：`substitute_assignments`（代課安排）資料表的權限規則規定「教務承辦人員」（非主管）不能直接寫入，要送審給教務主管——但代課安排頁面原本是直接 `supabase.insert()`，完全沒有走送審機制，加上 `lib/pendingChanges.ts` 的白名單也漏登記這張表，兩邊疊加的結果就是承辦人員一按新增就被資料庫擋下來，代課功能形同無法使用。

**修正**：
- 把 `substitute_assignments` 補進 `GOVERNED_TABLES` 白名單，頁面改用 `writeGoverned()`——主管/系統管理員S直接生效，承辦人員送審，核准後才生效並發通知。
- 整合原本各自獨立的「查詢空堂教師」跟「新增代課安排」：現在選好請假教師＋日期、勾選要代課的節次後，系統會**直接**用那個時段去查真正沒課的老師給你選，不用先查一次空堂、再手動記名字填到下面的表單。查不到空堂老師時，也可以切換成「從全體教師選擇」手動指定。

## 4. 聘書相關分頁（改動最大的一項）

比對過您提供的「0509教師資料_VBA列印.xlsm」原始活頁簿的公式設計後，確認：

- **(a) 批次修正**：「歷年教師資料」重新上傳同一份修正過的範本，現在會用姓名比對，抓到既有資料就**更新**（不是產生重複列），達到批次修正的效果（例如整批補上漏填的離職日期）。「在職時間」本來就已經會在沒填離職日期時自動統計到本日，這部分維持原樣。
- **(b)(c) 自聘教師資料／當年教師資料改為衍生資料**：不再是各自獨立維護的名單，姓名/職位/性別/離職狀態一律直接來自「歷年教師資料」的「自聘」勾選結果（勾了出現在自聘教師資料、沒勾出現在當年教師資料），這兩區只需要再補聘期/起迄/發聘時間這些聘書專屬欄位，不用重複輸入姓名資料。
- **(d) 列印樣本**：改用您這次上傳的「0509教師資料_VBA列印.xlsm」本尊（先前系統內建的樣本把資料工作表改了名稱，卻沒同步更新公式參照，導致列印出來全部空白，這次改成**保留原始活頁簿的工作表名稱**，程式改成配合這個名稱寫入資料，兩邊才對得起來）。並且**新增「上傳新樣本並永久取代」功能**——存進 Supabase Storage（見上方「套用步驟」第2點要先建立 bucket），上傳後所有人之後列印都會用這份新樣本，不再只是「這次瀏覽器分頁有效」。
- 另外也修正了「印自聘教師聘書」「印當年聘書」這兩個列印功能：原始樣本其實是用「起始序號～結束序號」印一個範圍，先前只設定了起始序號，結束序號會沿用樣本裡殘留的舊值，可能印出選單以外的人；已修正為起訖都設成同一個序號，只印選單選的那一筆。

## 5. 管理員A看不到任何學生資料

**根因**：這是系統性、不只影響單一帳號的問題。`sql/22department_policy_rewrite_complete.sql`（已在正式環境執行）把學生等資料表的權限規則從「角色判斷（admin_a/admin_b 就放行）」全面改成「部門職務判斷（要在 `app_user_departments` 有登記部門）」，但當時沒有同步處理「admin_a/admin_b 帳號本身要有部門職務才看得到資料」這件事——不管是既有帳號還是之後新建的，只要沒有部門職務，menu選單看得到、但查什麼資料都是空的。

**修正**：新增 `sql/35admin_role_department_backfill.sql`（**需要您手動到 Supabase 執行，見上方「套用步驟」**），做兩件事：
- 回補所有目前 role 是 admin_a/admin_b、但完全沒有部門職務的既有帳號，補上「教務＋訓導＋總務＋開發人員」四個部門的主管(lead)層級，恢復這兩個角色原本「全校管理」的能力。
- 新增資料庫觸發器，之後新建的 admin_a/admin_b 帳號會自動補上同樣的部門職務，不會再發生一樣的問題。

## 6. 一鍵上傳/下載缺分頁（訓導、教務、總務、開發者）

**根因**：「完整資料快照」下載/上傳原本只涵蓋教務處的8張表（帳號/班級/科目比重/任課教師/課表/節次/加扣分規則/學生）+ 排課系統 + 全校成績/出缺勤/獎懲，完全沒有涵蓋總務（庫存品項/庫存進出/修繕登記/水電費）、訓導（出缺勤示警門檻/獎懲加扣分參考值）、開發人員（學年學期設定，以及本輪新增的教師歷年資料/聘書）這幾個部門的資料表。

**修正**：新增 `lib/currentDataSheetsExtra.ts`（讀取現況）+ `lib/bulkHandlersExtra.ts`（批次上傳，同鍵值重複上傳＝修正既有資料），並串進 `components/dev-tools/BulkExcelPanel.tsx` 的「下載完整資料快照」與「一鍵上傳」流程，總共補上7張表（出缺勤示警門檻設定、出缺勤獎懲加扣分參考值、總務庫存品項、總務庫存進出紀錄、總務修繕登記、總務水電網路費用、學年學期設定），加上教師歷年資料/自聘教師資料/當年教師資料也一併納入快照。

**已知限制**：這7張新補上的表目前只有「現況」格式可以上傳（沒有另外做「全新學校、空白範本」版本），全新建校時這幾張表要先用「下載完整資料快照」拿到空的「現況」表頭格式再填資料，不能像其他8張教務表一樣用「下載全部表格範本」。如果之後有全新建校需求，可以再補上空白範本產生器。

---

## 本輪修改/新增的檔案

- `lib/currentDataSheets.ts`（修正帳號名單信箱讀取失敗）
- `components/dev-tools/BulkExcelPanel.tsx`（串接信箱查詢、串接新增的7張部門表）
- `app/(app)/admin/substitute-teaching/page.tsx`（整篇重寫：走送審機制、整合空堂查詢）
- `lib/pendingChanges.ts`（補上 substitute_assignments 白名單）
- `lib/teacherLetters.ts`（自聘/當年教師資料改衍生、批次修正、樣本永久上傳）
- `components/dev-tools/TeacherLettersPanel.tsx`（配合上面改版）
- `components/ExcelUploadButton.tsx`（顯示「更新」筆數）
- `public/templates/teacher_letters_print_template.xlsm`（換成0509原始樣本）
- `lib/currentDataSheetsExtra.ts`、`lib/bulkHandlersExtra.ts`（新增，訓導/總務/開發人員7張表）
- `sql/35admin_role_department_backfill.sql`（新增，**待您手動執行**）
- 移除 `上傳檔案範本/` 資料夾（過時測試檔案）

已用 `npx tsc --noEmit` 檢查過，本輪修改的檔案沒有型別錯誤（專案裡另外還有2個跟這次改動完全無關、原本就存在的型別錯誤，在 `app/api/reports/report-card/` 底下，不影響這次的修正範圍，可以之後再處理）。
