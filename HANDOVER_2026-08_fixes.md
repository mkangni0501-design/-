# 2026-08 修正交付說明（對應使用者提出的 7 項問題）

執行 SQL 順序照檔名數字；這次新增 `sql/32inventory_low_stock_threshold.sql`，
記得在 Supabase 執行（只是加一個欄位，不影響現有資料）。

## 1. 校服庫存販賣表、簿本庫存販賣表 → 改成【總.zip】v9.html 的內容
- 新增 `components/general-affairs/SalesInventoryPage.tsx`，取代 `/admin/general/uniforms`、
  `/admin/general/notebooks` 兩頁原本用的 `InventoryAdminPage`（書庫頁不受影響，維持原樣）。
- 完整重製 v9.html 的功能：販賣/採購/表格下載三分頁、今日收入/支出與本次結帳總額儀表板、
  庫存不足自動放大紅字＋側邊欄警示清單、樣本圖片上傳預覽（跟原型一樣只在瀏覽器本機預覽、
  不會上傳保存）、現有商品快速補貨、新商品自動上架、品項名稱/單價/警戒值即改即用設定表、
  每日統計＋交易明細歷史紀錄、依單價分組的 Excel 格式表格（多加了「下載 Excel」按鈕，
  原型只有「直接列印」）。
- 資料庫加了 `general_inventory_items.low_stock_threshold`（警戒值），原本沒有這個欄位。
- 跟原型不同：這裡接的是 Supabase 真實資料（不是瀏覽器記憶體，重新整理不會消失），且
  「總務承辦人員」的新增/結帳/補貨會照全站慣例送審，主管核准後才真正生效。

## 2. 學籍資料下載樣本 → 改成下載目前學生學籍資料
- `components/admin-tabs/StudentsImportTab.tsx`：下載按鈕改成先查「這個學年度/學期的在學
  學生現況」，有資料就下載現況（可以直接改一改重新上傳），查無資料才退回原本的空白範本。
- 新增學年度/學期欄位（預設帶入系統目前生效的學年學期）。

## 3. 一鍵清空：直接刪除，方便清空測試資料
- 這個功能本來就有（`/api/admin/wipe-term-data`），但清空前一定會先跑一次全校完整備份，
  資料量大時容易在伺服器逾時、導致清空失敗。
- 新增「略過清空前備份」勾選（預設不勾），勾選後直接刪除、不備份，加快速度、不容易逾時；
  正式資料建議還是保持不勾選（沒有備份就無法還原）。

## 4. 切換身分
- a. 全站共用的 TopNav 現在會明確顯示「目前視角：管理者視角／教師視角」。
- b. 找到根因：`attendance/mobile`、`attendance/weekly`、`attendance/report`、
  `reports/school-attendance` 四頁原本用寫死的角色陣列判斷管理員，沒有呼叫正確的
  `isAdminInCurrentView()`，導致切換到教師視角後這些頁面仍停留在管理者邏輯，已全部修正。
  （已排查全站同類寫法，`admin/accounts`、`admin/layout`、`admin/page` 等純管理員專屬頁面
  維持原樣不需要跟著視角切換。）

## 5. 新增「學生名冊」分類
- 新頁面 `/admin/students/roster`（`components/admin-tabs/StudentsRosterTab.tsx`），
  選班級直接看座號/學號/姓名，點列展開學生個人資料（唯讀）；已註冊進管理後台六大分類清單
  （教務／教師都看得到）。

## 6. 帳號管理
- a. 找到根因：新增帳號時如果 Auth 登入帳號建立成功、但緊接著寫入 `app_users` 失敗，
  就會留下「孤兒帳號」——信箱在 Auth 裡已存在（擋住重新新增），但 `app_users` 沒有對應
  資料列，帳號清單（只讀 app_users）完全看不到、沒辦法管理。
  已修正 `invite-user` API：失敗時自動刪除剛建立的登入帳號復原，避免以後再發生。
  另外新增「孤兒帳號」查詢/清理區塊（帳號管理頁最上方，只有系統管理員S看得到），
  可以直接找出並刪除已經卡住的舊孤兒帳號，讓信箱能重新使用——這是解決「現在已經卡住的」
  那個帳號的關鍵，請系統管理員S登入帳號管理頁面查看。
- b. 系統管理員S現在可以新增另一位系統管理員S（原本設計限定全系統只能1位，已依需求開放）。
  「下載樣本」改成「有帳號資料就下載目前帳號（含信箱，密碼欄一律留空）、沒有才退回範本」。

## 7. 教師資料檢查／合併，合併會出現錯誤提示
- 找到根因：合併教師時要把「要合併掉的那幾筆」所有外鍵參照都改指到保留的那一筆，最後才能
  刪除多餘資料；但清單漏了 3 個後來才加的資料表/欄位（`substitute_assignments` 代課登記的
  原任課/代課教師、`attendance_notifications.decided_by`、`staff_notifications.teacher_id`）。
  只要漏一個，最後刪除那一步就會被資料庫外鍵限制擋下來，出現錯誤提示。已補齊清單；同時讓
  「資料表根本不存在」（代表對應功能的 SQL 還沒執行過）安靜略過，不會誤報成合併失敗。

---
以上所有修改都已跑過 `tsc --noEmit` 全專案型別檢查，除了兩個跟本次修改完全無關、
本來就存在的既有型別錯誤（`report-card` 相關路由）以外，沒有新增任何型別錯誤。
